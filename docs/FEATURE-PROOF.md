# Feature proof ledger — every claim, its category, its proof

> **The single canonical claim → proof table.** Every feature kriya presents anywhere (README,
> website, docs) appears here **once**, categorized **SEE / CONTROL / PROVE** (the on-device
> control-and-proof product) or **EGRESS** (the outbound-governance set), with the concrete
> artifact that proves it: a test suite, a code path, a signed sample, a screenshot, or a public
> release. **Rule: a feature may be claimed only as far as its proof cell is real.** If a claim
> and this ledger disagree, the ledger wins; if this ledger and [`TRUST.md`](TRUST.md) disagree,
> TRUST.md wins.
>
> Companion: [`FEATURES.md`](FEATURES.md) — the same features in plain words.
> Current release: **Console v0.4.0**
> ([public DMG](https://github.com/governex/kriya-console/releases/tag/v0.4.0)) ·
> auditor CLI **audit-v0.1.0**.
>
> **Post-release rows:** a row tagged `✅ (main; ships vNEXT)` is merged and test-proven but
> **not yet in a released DMG** — the next notarized cut re-tags it. Until then, never present it
> as shipped-to-users. *(v0.3.2, 2026-07-25, re-tagged the doc-27/28 harvest rows — C1/C2 spend, B1 replay, F3 OTel, F1 local inference, F12a C2PA, F10 diode, F6-T1 measurement, F4-wasm, A4 FIPS, A5 PQ, D3 ISO 42001. v0.3.3, 2026-07-28, re-tagged A17 F3-fleet, A18 D1 memory, A19 B4 temporal, A20 B3 baselines. v0.3.4, 2026-07-28, re-tagged the R30 gates wave — A1.13 action gates, A1.14 policy packs, A2.11 Today. v0.4.0, 2026-08-06, re-tagged the v0.4.0 wave — A1.15 payments, A2.12 launcher, A2.13/A2.14 Analytics + charts, A21 shift attestation.)*

**Status legend:** ✅ shipped (in the current DMG) · 🧭 roadmap (designed, not built) ·
🕓 demand-gated (built when a design partner needs it).
**Proof types:** `test:` a suite in this repo (`npm test` / `cargo test`) · `code:` the
implementing path · `artifact:` a signed sample or release anyone can re-verify · `shot:` a
captured screenshot · `open:` proven in the public [kriya](https://github.com/governex/kriya)
runtime repo.

---

## PART A — NON-EGRESS (shipped: control + proof, device → fleet)

### A1. Govern — the enforcement layer (open runtime, free)

| # | Feature | Status | Proof |
|---|---|---|---|
| A1.1 | Governance gateway — wrap any MCP server, zero changes; policy → approval → budget → signed receipt on every tool call | ✅ | `open:` kriya repo `kriya-gateway` + its tests; receipts it emits verify in `test:verify.test.ts` |
| A1.2 | Govern Claude Code — PreToolUse hook on every tool call (subagents + headless verified) | ✅ | `open:` kriya repo `kriya-hook` (isError fix landed 2026-07-03); `shot:docs/screenshots/connections.png` |
| A1.3 | Govern Hermes — gateway (zero-change) + native-tool hook since v0.2.2 | ✅ | `open:` kriya repo `kriya-hermes-hook` |
| A1.4 | Govern desktop / no-API apps (computer-use, accessibility tree) | ✅ | `open:` kriya runtime reach-in drivers; demo `demo/kriya-gui-demo.mp4` |
| A1.5 | Policy engine — ordered allow / require-approval / deny, deny-by-default, **fail-closed on kriya's own errors** (B0 fixed + regression-tested) | ✅ | `test:policy.test.ts` · `code:src/lib/policy.ts` · B0 regression matrix · `shot:docs/screenshots/policy.png` |
| A1.6 | Human approval gate — pause for a person, 300s self-bound, fail-closed on timeout | ✅ | `open:` runtime ApprovalGate tests · `test:approvals.test.ts` (Console queue/record) |
| A1.7 | Budgets & rate caps (denial-of-wallet stop) — **egress-parity item B15, already shipped** | ✅ | `test:budget.test.ts` · `shot:` Budget view via `npm run capture` |
| A1.8 | Ed25519-signed, hash-chained receipts (`prev_hash` inside the signed bytes) | ✅ | `test:verify.test.ts` (TS↔Rust byte parity on real signed bytes; tamper → red) · `code:src-tauri/crates/kriya-verify` |
| A1.9 | SDK `registerAction` / `wrapAction` for in-process governance | ✅ | `open:` kriya repo SDK tests; Actual Budget integration (~37 lines) |
| A1.10 | Broad agent coverage (Claude Code, Hermes, any MCP client, desktop) — **egress-parity item B17, already shipped** | ✅ | union of A1.1–A1.4; Coverage Map lanes (A2.3) |
| A1.12 | **The governed CI lane — `kriya-ci`** (S4): a wrapper that runs a governed agent step in CI under a **repo-committed policy**, **fails the build when the policy blocks a governed action**, and leaves the signed receipts as a build-evidence artifact re-provable **offline** by `verify-receipts`. Distinct exit codes (0 clean · 3 policy-denied+named · 4 kriya-error · 5 step-failed). **Fail-closed:** a missing/invalid policy or unreadable/chain-broken log exits 4 — never a permissive default (the B0-class bug this lane must not repeat). Ships a std-only `kriya-ci` bin, a composite GitHub Action, an API-key-free example, and `docs/GOVERNED-CI-LANE.md` (with the GitLab recipe as config). **Named apart from I3 "Policy CI"** (counterfactual replay) and cross-linked — `kriya-ci` enforces the run's *own* policy over the run's *own* receipts. **Honest ceiling, stated:** the CI runner is a **cooperative** lane — it governs + evidences a step, it does **not** contain it (that's `kriya-gateway run --`, B14); the tamper-evidence is the offline re-prove, not trust in the runner. | ✅ *(public runtime `main` `9e98673`)* | `open:` `kriya-ci` bin unit tests (allow=0 · deny=3+named · require-approval-block · `kriya.*`-metadata-skip · missing/invalid-policy=4 · chain-break=4) + `kriya_ci_smoke` (real `kriya-ci`+`kriya-govern` over a scripted agent: clean=0 verifiable · deny=3 · missing-policy=4) · `artifact:` **REAL GitHub Actions run** [29900602638](https://github.com/governex/kriya/actions/runs/29900602638) — governed step → 944-byte receipts artifact → `verify-receipts` exit 0 offline → **one-byte tamper caught (exit 1)** · `code:` `docs/GOVERNED-CI-LANE.md`, `.github/actions/kriya-ci` |
| A1.11 | **SDK middleware — govern in-process agent-framework tool calls** (S2): wrap a framework's tool callback so every call is policy / approval / budget-gated and signs a receipt — **without an MCP hop and without inverting the framework's loop** — by delegating to the new **`kriya-govern`** binary (the one Signer; the wrappers contain no crypto, no policy engine, no chain writer). Shipped in **both languages** — `kriya-agents` (TS) and `kriya.agents` (Python, zero-dep) — each with the framework-agnostic `govern()` core **plus a verified, live-accepted adapter for every framework in the matrix**: **LangGraph** (py+js), **OpenAI Agents SDK** (py+js), **CrewAI** (py), **Claude Agent SDK** (ts+py). Each adapter is a thin, dependency-free shim verified against that framework's *current* tool seam and driven through the framework's own invocation path in tests. **Honest ceiling, in every README:** in-process governance is **cooperative** (a hostile process can skip it → containment / B14); it governs the **action tier + signs**, and does NOT see the tool's own egress. | ✅ *(public runtime `main`; npm/PyPI package publish pending)* | `open:` `kriya-govern` bin (6 Rust tests) + `kriya-agents` (**11 vitest**: core + LangGraph + Claude Agent SDK + OpenAI Agents, driving real frameworks) + `kriya.agents` (**66-test** suite incl. LangGraph/OpenAI/CrewAI/Claude adapters) — all vs. the REAL bin · `artifact:` stub-agent receipts verify in THIS Console's `verifyReceipt` (ok; tampered byte → fail) |

| A1.13 | **Action gates — the high-stakes classes behind one dial each** (F-2, doc 31): deploys · destructive git · publishes(+dep installs receipted, never blocked) · production DB · infra · outbound sends · **self-modification (default Deny — the prompt-injection vector)**, each Allow / Receipt-only / Approve / Deny. Console-compiled matcher rules (`gates.rules` in `agent-policy.yaml`) evaluated by `kriya-hook`'s pre lane as a **tighten-only** escalation over the action tier (never loosens a deny); `approve` runs the existing approval gate; every decision a signed `kriya.gate.<class>.{evaluated,held,approved,denied}` receipt with the blocked attempt's own action receipt chain-linked (`corr_step`) — the gate receipt itself never carries command content. **One matcher table, two consumers** (the F-1 severity lens + this engine), parity-locked TS↔Rust by committed cross-repo vectors. **Honest lane, stated in the UI:** enforced in the Claude Code hook lane; other lanes are observed, not blocked. The `payment` class is now a **primary enforced gate** (see A1.15) — the disabled-preview card is gone. | ✅ **v0.3.4** | `open:` `permissions::GatePolicy` (7 unit tests incl. doc-30 incident shapes) + `kriya_hook_smoke` gates cases (**compiled-binary B0 discipline**: self-mod deny → exit 2 + signed action+gate receipts; receipt tier proceeds; approve tier holds) · `test:gates.test.ts` ×47 (28 cross-repo vectors evaluated by BOTH implementations over the committed `default-gates.yaml`; drift-lock; dialect guard; BC-3 round trip) · `artifact:test/fixtures/gates_ledger.jsonl` (Rust-signed, every verb, TS-verified) · **release-artifact check (v0.3.4):** the DMG-bundled hook, run against a gates policy, denies a `.claude/settings.json` edit with exit 2 + `kriya.gate.self-mod.denied` on the chain · `shot:docs/screenshots/policy-gates.png` |
| A1.15 | **Agent payment governance — on-device, pre-egress: the signed intent→approval→transaction chain** (F-4, doc 31 / doc 33 §4-A): the `payment` gate class promoted from view-only to a **primary enforced dial** (Allow / Receipt-only / Approve / Deny). Payment-shaped calls (processor SDKs / tools / hosts, `matchers.ts`) are evaluated by `kriya-hook`'s pre lane through the **class-agnostic `GatePolicy`**, so `kriya.gate.payment.*` gate receipts flow with **no runtime gate-code change**. A new **content-free** `kriya.pay.{intent,decision,outcome}` vocabulary chains a payment across the fresh pre/post hook processes by a **deterministic `pay_id`** (session+tool+tool_input) — the post-hook outcome links onto the pre-hook intent with **no marker file**; the amount is **best-effort and honest** (integer minor-units + currency ⇒ `amount_known:true`, else `false`, **never a guessed number**); merchant = the processor/host string only; **card custody stays in credential brokering (B13 / EG-B) — no PAN ever in a receipt**. Denied ⇒ the full 3-link chain closes synchronously; held ⇒ the honest 2/3 shape (no outcome); proceeding ⇒ the post hook writes the real `executed｜denied`+status outcome from the tool response. Console: Spend › **Usage / Purchases** tabs (merchant · amount · agent · decision-chip · I→D→O ✓-steps → receipt drawer) + a **PaymentBadge** on `kriya.gate.payment.*` approvals (amount vs cap + day-spend; over-cap shown typographically; honest-unknown). **Naming (doc 24 §11.5):** enforced on the governed Claude Code hook lane only — other lanes observed; never "PCI"/"DLP." Additive+optional params on the frozen envelope — old receipts verify byte-unchanged. | ✅ **v0.4.0** | `test:purchases.test.ts` ×11 (per-`pay_id` fold, honest-unknown amount, verified/complete flags) + `test:approvals.test.ts` (PaymentBadge amount-vs-cap guards) · `artifact:test/fixtures/pay_ledger.jsonl` (Rust-signed via `../experiment1 tests/pay_fixture.rs`, fixed RFC-8032 key, **TS-verified byte-identical** to the runtime copy) · `open:` `crates/kriya/src/pay.rs` unit tests (deterministic `pay_id`, honest amount, param builders, `status_of`) + `kriya_hook_smoke` payment e2e (real binary: denied full chain · receipt-tier pre→post executed w/ status · held 2/3 unknown-amount) · `test:test/otel-semconv.test.ts` (`kriya.pay.*` registry + mapping, content-free/no-PAN) · `code:src/lib/purchases.ts`, `src/lib/matchers.ts`, `src/views/SpendView.tsx` · `shot:docs/screenshots/spend-purchases.png` |
| A1.14 | **Policy packs — Developer / Analyst / Planner** (F-3, doc 31): named, **versioned** presets over the gates (+ an egress-lock lane choice), applied to the device in one click through the SAME gates compiler as manual edits; the application/edit is itself a signed `kriya.policy.pack.{applied,changed}` receipt on the Console's own chain (ids/names/versions only, never rule content). Duplicate-then-edit customs; per-identity assignment recorded as labeled bookkeeping for the launcher/fleet ("one policy file per device today" — the UI says so); `bundleRef` reserved for doc-22 PolicyBundle embedding, distribution NOT built. | ✅ **v0.3.4** | `test:packs.test.ts` ×17 (every preset's FULL gate matrix asserted; apply touches only gates+packs(+egress-lock preserving host rules); assignment YAML round-trip) + `test:pack-receipts.test.ts` (Console-Rust-signed fixture verifies + chains + classifies Tier-2) · `code:src/lib/packs.ts`, `src-tauri/src/packs.rs` · `shot:docs/screenshots/policy-packs.png` |

### A2. See — the live cockpit (Console, free)

| # | Feature | Status | Proof |
|---|---|---|---|
| A2.1 | Monitor — auto-tail `~/.kriya/audit/`, re-verify every receipt on-device, live posture | ✅ | `code:src/views/Monitor` + `src-tauri/audit.rs` · `shot:docs/screenshots/monitor.png` |
| A2.2 | Audit log — per-receipt signature check, tampered/forged rows red, chain-break flagging | ✅ | `test:verify.test.ts` · `artifact:` tamper-a-byte demo in release audit-v0.1.0 · `shot:docs/screenshots/audit.png` |
| A2.3 | Coverage Map — six lanes GREEN/AMBER/GREY; every state change is itself a signed `coverage.snapshot` receipt in its own chain | ✅ | `test:coverage-fixture.test.ts`, `p2-era-coverage-fixture.test.ts` · `code:src-tauri/coverage.rs` |
| A2.4 | Govern All — detect agents on the machine, wire hooks + gateway + policy in one click, reversibly | ✅ | `test:govern.test.ts`, `govern-view.test.ts` · `code:src-tauri/govern.rs` |
| A2.5 | Connections — managed MCP wiring, `claude_desktop_config.json`, macOS permission walkthrough | ✅ | `code:src/views/Connections` · `shot:docs/screenshots/connections.png` |
| A2.6 | Guided setup / onboarding | ✅ | `code:src-tauri/onboarding.rs` |
| A2.7 | **Policy CI — "test before apply"** (I3, doc 26): replay a candidate policy over this device's own re-verified receipts and show which past actions would get a **different tier** under it (e.g. newly denied / approval-gated) — "this change would have changed N of last week's M actions." Free; both single-device (`PolicyView`) **and** fleet pre-publish (`ControlPlanePolicyTab`) call the one command. **Honest scope, stated in the UI:** the **action-tier gate only** (allow / approval / deny, incl. the B11 read-only pre-empt) — budget, egress-tier, and detection-pack body/host heuristics are **not** replayed; the baseline is the current on-disk policy; the simulation itself is a signed, chained `kriya.policy.sim.result` receipt (auditable-by-design). Distinct from S4's "governed CI lane" (`kriya-ci`), which runs an agent in CI. | ✅ **v0.3.0** | `test:` `simulate::tests` ×11 (`kriya-verify`, incl. B11 pre-empt) + `policy_sim::tests` ×7 (over real signed fixtures) · `code:src-tauri/src/policy_sim.rs`, `src-tauri/crates/kriya-verify/src/simulate.rs` · `shot:docs/screenshots/policy-sim.png` |
| A2.8 | **Broker one-click — Cursor · Cline · GitHub Copilot · Gemini CLI** (S1, doc 21 GA / W2-4): Govern All detects each VS-Code-family / CLI MCP client and one-click routes its stdio MCP servers through `kriya-gateway` — idempotent, non-clobbering, faithfully reversible (the exact Claude-Desktop path, generalized). Config seams **verified 2026-07-21** against each agent's current docs: Cursor/Cline/Gemini use `mcpServers`; **Copilot's map is keyed `servers`** (lowercase); Gemini's remote key is `httpUrl`. **Honest ceiling (in the Coverage/Connections copy):** the MCP lane is governed via the gateway, but each agent's **native built-in tools bypass MCP — ungoverned unless launched under containment (B14)**; cloud/off-device agents (Cursor background, Copilot's cloud coding agent) are out of scope (locus rule). Per-agent acceptance: **Cursor** (installed on this Mac) — detected + wired + reverted **live**; **Cline / Copilot / Gemini** — wired + fixture-tested, live-install acceptance pending a real install. | ✅ **v0.3.0** | `test:` `govern::tests` (servers-key / detection / idempotent-wrap + byte-for-byte unwire, ×6) + `coverage::tests` (per-agent GA-2 lanes, ×2) · `test:tests/govern_live_smoke.rs` (real `govern_all`/`ungovern_all` on all four, staged gateway, byte-stable round-trip) · `code:src-tauri/src/govern.rs` (`Client`), `src-tauri/src/coverage.rs` (GA-2) · `shot:docs/screenshots/connections-agents.png` |
| A2.9 | **Codex CLI — verified seam verdict (not yet wired)** (S1): OpenAI Codex CLI has a real local seam — `~/.codex/config.toml`, MCP servers under `[mcp_servers.<id>]` with `command`/`args`/`env`/`cwd` (stdio) or `url` (remote), **verified 2026-07-21** against the Codex config reference. It is **TOML**, not JSON/YAML — wiring needs a TOML reader/writer the console doesn't ship yet, so this is **recorded, not built**: no Codex wiring is claimed. Not out-of-scope (the seam is real + local); a scoped follow-up adds the TOML edge. | 🧭 roadmap | `code:` the `Client` doc-comments record the verified seam · dated verdict here |
| A2.10 | **Sessions — run correlation** (S3, the substrate for doc 26 I2/I5): optional, params-level `kriya.corr` on governed-lane receipts — `run_id` (session grouping) · `parent_step_id` (explicit lineage) · `agent_id` (Claude Code's sub-agent discriminator) — designed **once** so I2's source-set and I5's session-ABOM extend it, not a second vocabulary. Emitted by every governed lane, honestly per-seam: **hook** = `run_id`+`agent_id`, **never a fabricated `parent_step_id`** (the Claude Code payload carries no parent pointer, verified 2026-07-22); **SDK middleware** = `run_id` per invocation + `parent_step_id` for nested calls (placement centralized in `kriya-govern`); **gateway/broker** = per-session `run_id` (one process = one client session). The Console **Sessions** view rebuilds the `run → sub-agent → action` tree from **VERIFIED receipts only**, and the Evidence export gains an **optional** correlation appendix. **Frozen schema:** correlation rides `params`, additive+optional — a receipt without it signs byte-identically and every existing verifier keeps validating (TS↔Rust byte-parity proven on real signed `kriya.corr` receipts). **Honest ceilings, unprompted:** run ids are pseudonymous but linkable, so they live in `params` — **structurally never in an off-device envelope** at any verbosity (the sealed minimizer reads only `action_id`+`success`; hostile fixtures guard it); the hook can't see a parent step so it emits none; correlation is **cooperative** (same ceiling as A1.11). | ✅ **v0.3.0** | `test:` `sessionTree.test.ts` ×11 (orphans/missing-parents/interleaved/clock-skew/cycles) + `compliance.test.ts` (appendix present-when-correlated / byte-identical-when-absent) + `verify.test.ts` (TS verifies real Rust-signed `kriya.corr` + tamper→fail + legacy still verifies) · `open:` `kriya::corr` (attach/anti-forgery, ×10) + hook/govern/governor correlation tests + `kriya_hook_smoke` W0-3 (real binary over pinned contract fixtures) + `kriya-agents`/`kriya.agents` run-id tests · `test:` `paid.rs::correlation_summary` (Rust export parity) + redaction hostile fixtures (`kriya-verify` + `control_plane/redact`, secret-in-run_id → never in a window) + `kriya-verify::receipts` corr round-trip · `code:` `src/lib/sessionTree.ts`, `src/views/SessionsView.tsx` · `artifact:test/fixtures/s3-corr-audit.jsonl` (real Rust-signed correlated + legacy receipts) · `shot:docs/screenshots/sessions.png` |

| A2.11 | **Today — the default landing view + the severity layer** (F-1, doc 31 / doc 30 R30-1): the last 24 h as human-readable **headlines** over verified receipts (prod deploys, publishes, releases/signing, denies; dep installs aggregated per source+hour), needs-you approval cards off the same queue as Approvals, and the day's posture strip (agents, chain re-check, pack in force, spend). The Audit log gains the severity lens (Headline · Notable · Denied · per-class chips; per-hour Tier-3 rollups) with the classic ledger **byte-identical under "All"** (identity-locked test); one shared receipt drawer (claim, signature verdict, live chain re-check via `chain.ts`, payload, raw-line export) from Today/Audit/Approvals. **Honest wording (doc 24 §11.5): "Blocked"/"Held" only on explicit runtime deny/hold receipts** — a bare `success:false` renders "did not complete". View-side classifier only: no schema change, no runtime change. | ✅ **v0.3.4** | `test:severity.test.ts` ×87 (one fixture per taxonomy row incl. the doc-30 incident shapes; honest-wording rules; rollup/lens logic; the All-chip identity regression) + `test:landing.test.ts` · `code:src/lib/severity.ts`, `src/lib/matchers.ts` (the ONE table the gate engine also compiles from), `src/components/ReceiptDrawer.tsx` · `shot:docs/screenshots/today.png`, `audit-severity.png`, `audit-denied.png` |

| A2.12 | **Governed run launcher — `kriya-run`** (F-5, doc 31 / doc 33 §4-B): a new auto-discovered runtime bin that **composes pack + lanes + agent, signs ONE `kriya.run.launched` attestation** via the one-Signer path, then **execs the agent command**. Copy-first (v1): it is **not a second enforcement path** — per-call governance still flows through the agent hook (the honest ceiling, stated). Params are **content-free** — `{v, agent, pack, lanes, shift}` only, **never argv or cwd**. Console: Start › **New governed run** — `LaunchView` composes `agent × pack × cwd × lanes` into a live `kriya-run …` command + Copy + a recent-launches list; the Sessions `RunCard` gains a `pack:<id>` chip when a launch started the run. | ✅ **v0.4.0** | `open:` `crates/kriya/src/bin/kriya-run.rs` — 9 unit tests (pure parse/params) + `artifact:` `tests/fixtures/run_launched_ledger.jsonl` via `tests/run_launched_fixture.rs` (Rust-signed, fixed RFC-8032 key) mirrored **byte-identical** to `test/fixtures/run_launched_ledger.jsonl` and TS-verified · `test:launch.test.ts` ×10 (`composeRunCommand` determinism, `foldLaunches`/`launchForRun`) · `test:test/otel-semconv.test.ts` (`kriya.run.*` registry + mapping) · `code:src/lib/launch.ts`, `src/views/LaunchView.tsx` · `shot:docs/screenshots/launch.png` |

### A2 (cont.) — Analytics & SLOs (O-wave, Console, free — view-side over verified receipts)

> The whole Analytics surface is a **view-side derivation over already-verified receipts** — no
> schema change, no runtime change, no new receipt type. Every number is a threshold or a count;
> **there is no composite risk score anywhere** (the honesty counterpart to competitor "risk
> scores"). A `success:false` receipt renders **"did not complete," never "blocked"** — the shipped
> severity wording law (doc 24 §11.5); "Blocked"/"Held" stay reserved for explicit deny/hold receipts.

| # | Feature | Status | Proof |
|---|---|---|---|
| A2.13 | **Monitor › Analytics — Reliability · SLOs · Posture** (O-1 / O-4 / O-12, doc 33 §3): a new tabbed view. **Reliability** — actions · success rate · **deny/hold split** (single-sourced from `severity.receiptDisposition()`) · failures, per-LOCAL-day series + per-tool / per-agent folds + `error.type` facet chips; every tile/bar/row click-throughs to a pre-filtered Audit and opens the receipt drawer. **SLOs** — approval-latency p50/p95 paired from `kriya.gate.<class>.held → approved｜denied` by `corr_step` (an open hold is `pending`, never a latency sample), verification pass rate (the loader's own `outcome.ok`), deny rate (imported from `reliability.ts`), budget headroom (`summarizeBudgetStatus`, rendered not re-derived), heartbeat-gap count (folded `kriya.watch.heartbeat` cadence `>2×` median **or** signed `kriya.attest.shift.gap` receipts when present — feature-detected). **Posture** — week-over-week threshold **crossings only** (denies · Tier-1 headlines · gaps · unapproved model digests · budget breaches · unverifiable receipts) with the fixed caption verbatim: *"Evidence posture — counts from verified receipts. Not a risk score."* | ✅ **v0.4.0** | `test:reliability.test.ts` ×9 (gate/io/spend deny, `success:false`, mixed week) + `test:slo.test.ts` ×10 (paired/unpaired holds, cross-class, gap detection, WoW incl. empty prior week) + `test:posture.test.ts` ×8 (WoW crossings, **no-composite** assertion, the "Not a risk score" string asserted) + `test:views-charts.test.ts` (AnalyticsView across ranges; deny-vs-failed wording law) · `code:src/lib/{reliability,slo,posture,models}.ts`, `src/views/AnalyticsView.tsx`, `src/lib/severity.ts` (`receiptDisposition`, `failed` audit chip) · `shot:docs/screenshots/analytics.png`, `analytics-slo.png`, `analytics-posture.png` |
| A2.14 | **The chart layer + Spend Trend + Local-Models latency** (O-2, doc 33 §3): the Console's first charts — **hand-rolled SVG, zero new dependencies**: three primitives (`Sparkline`, `BarSeries`, `DistBar`), `currentColor` + existing CSS variables only, `<title>`+aria-label, deterministic (`nowMs` injected). **Severity color (green/amber/red) stays RESERVED for verified/pending/denied and never appears in a chart.** One shared `bucketByDay`/`bucketByHour` fold (local-calendar boundaries → DST-safe; zero-buckets kept so gaps show honestly). Consumers: Spend › **Trend** (30-day priced-spend/day + tokens/day, bucketed straight off `summarizeSpend`'s latest-per-session set — a superseded settle can't double-count) and Local Models per-model **p50/p95 latency + serves/day** from `kriya.model.serve` `duration_ms` (a zero-duration model reads "no latency data," never a fabricated 0 ms). | ✅ **v0.4.0** | `test:timeseries.test.ts` ×8 (empty, single bucket, DST-count) + `test:charts.test.ts` ×9 (SVG render via `renderToStaticMarkup`, no jsdom) + `test:views-charts.test.ts` ×10 (Spend Trend `$` bars; latency p50/p95 + no-latency path from real fixtures) · `code:src/lib/timeseries.ts`, `src/components/charts/{Sparkline,BarSeries,DistBar}.tsx`, `src/views/SpendView.tsx`, `src/views/LocalModelsView.tsx` |

### A3. Decide & attribute (Console, free)

| # | Feature | Status | Proof |
|---|---|---|---|
| A3.1 | Approvals queue — cross-app, risk-ranked, RBAC-gated; decision + reason recorded (a queue/record; live remote-unblock = P7, 🕓) | ✅ | `test:approvals.test.ts` · `shot:docs/screenshots/approvals.png` |
| A3.2 | Identity & access — per-operator / per-agent dashboards from the signed `actor`; roles admin/approver/operator/viewer | ✅ | `test:identity.test.ts`, `actor.test.ts` |
| A3.3 | Budgets & rate view — usage vs caps, at-limit history | ✅ | `test:budget.test.ts` |

### A4. Prove — the compliance tier (paid license; verification itself stays free)

| # | Feature | Status | Proof |
|---|---|---|---|
| A4.1 | Evidence export — 19 controls / 5 frameworks (NIST 800-171/CMMC AU 3.3.1–3.3.9, SOC 2, ISO 42001, EU AI Act, residency), statuses **computed from re-verified receipts**; 3.3.9 a permanent visible gap; footer "evidence, not a certification" | ✅ | `test:compliance.test.ts` · `code:src-tauri/paid.rs` · `shot:docs/screenshots/evidence.png` |
| A4.2 | Auditor CLI `kriya-audit` — offline re-prover (receipts, envelopes, kriyad read-back), exit 0/1 | ✅ | `code:src-tauri/crates/kriya-audit-cli` · `artifact:` public release **audit-v0.1.0** with sample evidence + tamper demo |
| A4.3 | Assessor sample pack — 28 receipts, 1 deliberately tampered, test-guarded, never in the build | ✅ | `artifact:docs/samples/au-family-sample` · `test:au-family-sample.test.ts`, `no-sample-in-build.test.ts` |
| A4.4 | Cross-app fleet correlation (this machine) — verified/failed, signers, policy coverage | ✅ | `code:src/views/Fleet` · `test:` rollups in `compliance.test.ts` |
| A4.5 | Offline license — Ed25519-signed token, no phone-home, no accounts | ✅ | `code:src-tauri/license.rs` (self-serve checkout 🕓 — R0) |
| A4.6 | **FIPS signing lane** (doc 27 A4) — opt-in `aws-lc-rs` `fips` feature (AWS-LC-FIPS 3.x, CMVP cert #5298) for receipt sign/verify, **OFF by default everywhere** (the free tier + notarized DMG are byte-unchanged); per-OS honest wording (Linux validated-OE / other-Linux+macOS same-module-code-untested-OE / default not-FIPS) never upgraded past the attested `operational_environment`; new signed, chained, additive `kriya.crypto.module` self-attestation (host provenance, never a proof — a signature can't reveal which module signed it, RFC 8032 determinism); byte-for-byte interop proven both directions (a FIPS-signed receipt verifies under a default-build verifier and vice versa, incl. the real `runtime-egress-ledger.jsonl` fixture). Cross-repo: the runtime's Ed25519 signer + this Console's Rust verifier/signer both route through a signature-identical `crypto` facade. | ✅ | `test:test/fips-parity.test.ts` (TS/WebCrypto verifies the FIPS-signed fixture + wording lint) · `cargo test -p kriya --features fips-crypto` (runtime, incl. `fips_fixture.rs`) · `cargo test -p kriya-verify --features fips-crypto` (console) · `code:../experiment1/crates/kriya/src/crypto.rs`, `src-tauri/crates/kriya-verify/src/crypto.rs` · `artifact:docs/samples/fips-module-boundary.md` · `doc:docs/TRUST.md` Cryptographic-module section |
| A4.7 | **PQ dual-signature receipts** (doc 27 A5) — opt-in `aws-lc-rs` `unstable` feature (ML-DSA-87, FIPS 204) countersignature next to Ed25519, **OFF by default everywhere** (the free tier + notarized DMG are byte-unchanged); **default mode = one countersignature per chain checkpoint** (`kriya.crypto.pq_checkpoint`, every N=256 receipts, measured ≈1.03–1.13× storage cost) sealing the whole prefix via the quantum-resistant SHA-256 chain; **opt-in per-receipt dual-sig** for low-volume high-value receipts (measured ≈14.6 KiB/line, ≈30× if applied to every line — why it's opt-in); the PQ key is bound to the pinned Ed25519 identity by a new `kriya.crypto.pq_key` attestation; wording is "post-quantum-ready (CNSA 2.0-aligned, ML-DSA-87)," **never** "FIPS-validated PQ" (ML-DSA is outside cert #5298's approved boundary) or "quantum-proof"; the TS/browser lane verifies Ed25519 + message parity + PQ structure only — the compiled Rust lane is the PQ-verifying authority (an honest, stated ceiling, never a false green). Byte-for-byte interop proven both directions incl. the real dual-signed + checkpoint fixtures; cross-implementation parity proven both directions against RustCrypto's independent `ml-dsa` (test-only, unaudited). **Deviation from the original design:** `pq-crypto` and `fips-crypto` are mutually exclusive in this build (a real `aws-lc-rs` upstream constraint — its `unstable` PQ module is compiled out whenever `fips` is active — not a kriya restriction; a `compile_error!` in both `crypto.rs` files catches an accidental double-enable). | ✅ | `test:test/pq-parity.test.ts` (TS/WebCrypto structural verify + matrix + wording lint) · `cargo test -p kriya --features pq-crypto` (runtime, incl. `tests/pq_fixture.rs`, cross-impl parity in `crypto.rs`) · `cargo test -p kriya-verify --features pq-crypto` (console, incl. cross-repo fixture parity in `receipts.rs`) · `cargo test -p verify-receipts --features pq-crypto` (offline CLI, incl. cross-impl parity) · `code:../experiment1/crates/kriya/src/crypto.rs`, `src-tauri/crates/kriya-verify/src/crypto.rs` · `artifact:docs/samples/pq-dual-signed-receipt.md` (real fixture receipts + measured size table) · `doc:docs/TRUST.md` Post-quantum-readiness section |

### A5. Fleet control plane (paid `fleet-console`; P0–P6 shipped)

| # | Feature | Status | Proof |
|---|---|---|---|
| A5.1 | `kriyad` aggregator — customer-run static binary (BOX/K8S/air-gap), mTLS everywhere, verifies all ingest, append-only, **authors nothing** (holds no keys) | ✅ | `code:src-tauri/crates/kriya-aggregator` · `cargo test --features control-plane` |
| A5.2 | Evidence Compiler + enforced redaction — allowlist drop-by-default minimized `AttestationEnvelope`s; params/operator names structurally cannot leave; hash-chained outbox | ✅ | `test:envelope.test.ts` · `code:control_plane/{compiler,envelope,redact,outbox}.rs` |
| A5.3 | Device inventory beacon — signed `DeviceInfo`, GDPR-allowlisted schema with **no field** for usernames/hostnames/IPs/serials | ✅ | `test:device-info-fixture.test.ts` · `code:control_plane/device_info.rs` (adversarially tested) |
| A5.4 | Fleet table cockpit — liveness, versions + update badges, agent chips, drill-in to signed chains | ✅ | `code:src/views/ControlPlane*` · `shot:docs/screenshots/fleet-*.png` (`npm run capture:fleet`) |
| A5.5 | Central policy authoring + org-key-signed downlink — pull on heartbeat, verify against a **pinned** org key, anti-rollback, signed `policy.applied` receipt; air-gap = signed file | ✅ | `test:policy-bundle.test.ts` · `code:control_plane/{org_key,policy}.rs` · `shot:fleet-policy-author.png` |
| A5.6 | Policy-drift view — verdict from each device's own re-verified envelopes; kriyad's row only a hint; mismatch badge on disagreement | ✅ | `test:policyDrift.test.ts` · `shot:fleet-drift.png` |
| A5.7 | Org-wide evidence export — AU fleet-wide + CM 3.4.1/3.4.2 from the signed policy chain; silent devices named as red cells | ✅ | `test:orgEvidence.test.ts` · `code:control_plane/fleet_evidence.rs` · `shot:fleet-org-evidence.png` |
| A5.8 | mTLS cert-role separation — device certs can't read the fleet; operator certs can't post evidence; fail-closed per route | ✅ | `cargo test` (aggregator route guards, P6) |
| A5.9 | Zero-egress attestation (air-gap posture only) — signed proof nothing left; free tier opens **no socket at all** | ✅ | dormancy guard (`code:src-tauri/control_plane.rs`; free build links no fleet networking — [`TRUST.md`](TRUST.md)) |
| A5.10 | Remote approvals (P7) — operator-signed verdict unblocks a paused device action | 🕓 | design partner gate |
| A5.11 | Enrollment CA/CRL · HSM issuer · MDM zero-touch · SSO/OIDC (phases 3–5) | 🕓 | demand-pulled |

### A6. The trust spine (cross-cutting, free — what makes every row above checkable)

| # | Feature | Status | Proof |
|---|---|---|---|
| A6.1 | TS ↔ Rust canonical byte parity on every signed artifact type (receipts, envelopes, device-info, policy bundles) | ✅ | `npm test` — fails on one byte of drift; cross-version fixtures in `test/fixtures` |
| A6.2 | Three independent verifications for evidence (device → kriyad ingest → cockpit/auditor); two for policy (ingest → device apply) | ✅ | union: `verify`, `envelope`, `policy-bundle` suites + aggregator `cargo test` |
| A6.3 | Published honest limits — tamper-**evidence** not proofing; pin your signer; seams fail-open on their owners' side; disclosed shipped bugs (B0) | ✅ | [`TRUST.md`](TRUST.md) (canonical) |

### A7. Verifiable Telemetry — the OTel GenAI bridge (F3, doc 28, free)

> **Scope actually shipped vs. the doc 28 spec — read before claiming this row.** F3's spec
> describes a live OTLP/HTTP push to a customer's collector and a live localhost OTLP/HTTP
> listener, both off-by-default runtime toggles with no license gate. Building either literally
> would link a network-capable dependency into the DEFAULT free Console binary for the first
> time, contradicting this repo's own dormancy-tested claim (`README.md`: "the free tier ... opens
> no network connection"; `TRUST.md`: "no socket to any server ... is ever opened"). Rather than
> silently resolving that architecture question, this build ships the network-free half only: the
> semconv mapping (both languages), the OTLP JSON construction, a **file** export/import sink, and
> the offline re-verifier — and flags the live-transport decision for the founder/architect (see
> the doc comment atop `src-tauri/src/otel/exporter.rs`). The gate text itself accepts a file
> exporter as sufficient manual proof alongside a live collector, so this is a complete, honestly-
> scoped slice, not a stub.

| # | Feature | Status | Proof |
|---|---|---|---|
| F3.1 | Semconv mapping module — every receipt maps to an OTel GenAI span; `gen_ai.*` names where a GenAI-semconv equivalent exists (OT-1), `kriya.*` names otherwise (OT-2); unknown action_ids fall back to a generic `execute_tool` span with `kriya.action.id` preserved | ✅ | `test:test/otel-semconv.test.ts` (TS) + `cargo test -p kriya-verify otel::` (Rust twin, `crates/kriya-verify/src/otel.rs`) · `code:src/lib/otel/{registry,semconv}.ts`, `src-tauri/crates/kriya-verify/src/otel.rs` |
| F3.2 | **OT-1 vocabulary-registry test** — every `kriya.*` action_id this codebase knows (`registry.ts`'s `KRIYA_VOCABULARY`) must carry a decided mapping entry; a new vocabulary added without one fails `npm test` | ✅ | `test:` `"OT-1 vocabulary-registry test"` in `test/otel-semconv.test.ts` |
| F3.3 | OTLP/HTTP JSON export construction — one session → one `invoke_agent` root span, each verified receipt → an `execute_tool` child span carrying `kriya.receipt.id`/`kriya.receipt.sig`/`kriya.receipt.raw`/`kriya.chain.head`/`kriya.schema.version`; deterministic hash-derived span/trace ids (OT-5, idempotent re-export) | ✅ | `test:` `build_export_is_byte_exact_over_a_golden_fixture`, `uncorrelated_receipts_still_export_under_a_synthetic_pseudo_run` (`src-tauri/src/otel/exporter.rs`) |
| F3.4 | Export sink — writes the OTLP JSON to a file (`~/.kriya/exports/` by default); never opens a network connection | ✅ | `code:src-tauri/src/otel/exporter.rs::export_to_file` · manual proof: `cargo run --example otel_demo` (real signed receipts → file export → offline re-verify, incl. a tamper) |
| F3.5 | The non-egress dial, receipted (OT-3) — enabling export signs one `kriya.otel.export.enabled` receipt into its own free-tier hash chain (`~/.kriya/audit/kriya-otel-events.jsonl`, own key `~/.kriya/keys/otel.key`), idempotent on repeat enable | ✅ | `test:otel_settings_round_trip_via_temp_home` |
| F3.6 | **`kriya-audit --verify-otel <file>`** — the offline re-verifier: decodes each span's embedded `kriya.receipt.raw`, re-checks the Ed25519 signature via the SAME `kriya_verify::verify_value` the Console/kriyad use, cross-checks `kriya.chain.head`/`kriya.chain.prev_hash` self-consistency and per-trace chain continuity; exit 0/1 | ✅ | `test:` `verify_otel_round_trips_a_genuine_two_span_chained_export`, `verify_otel_fails_on_a_tampered_span_with_the_right_reason`, `verify_otel_fails_on_a_broken_chain_continuity`, `verify_otel_rejects_a_non_otlp_file` (`kriya-audit-cli`) · manual proof: real `kriya-audit --verify-otel` run, exit 0 on a genuine export, exit 1 with the exact tamper reason on a corrupted copy |
| F3.7 | Ingest (P2) — import third-party OTLP/HTTP JSON (e.g. Claude Code's own OTel export) into a SEPARATE observations store, **never the receipt store** (OT-4); Sessions renders them visually stratified below receipt-backed rows (dashed border, hollow dot, "observed-unsigned" badge), with a "governance gap" count. File-import only in this build — see the scope note above | ✅ (file-import) | `test:` `parses_otlp_spans_and_derives_traceparent`, `ingest_stratification_labels_traceparent_heuristic_and_gap_matches`, `import_dedupes_by_trace_and_span_id_via_temp_home` (`src-tauri/src/otel/observations.rs`) · `code:src/views/SessionsView.tsx` (`ObservedSpansSection`) |
| F3.8 | Matching (OT-6) — `kriya.otel.traceparent` in a receipt's params when present (a real correlation), else an explicitly-labeled time+tool-name heuristic; never conflated | ✅ | same tests as F3.7 |
| F3.9 | Live OTLP/HTTP push to a customer collector + a live localhost OTLP/HTTP listener | 🧭 **flagged, not built** | see the scope note above — blocked on a founder/architect call, not a build gap |

### A8. Attested Local Inference — govern Ollama/vLLM/llama.cpp (F1, doc 28, free)

> **Bypass honesty, stated up front.** `kriya-llm-proxy` governs ONLY clients explicitly pointed at
> it (`OPENAI_BASE_URL`, `OLLAMA_HOST`, or a client's base-url setting). A process that connects
> straight to the upstream inference server (e.g. `http://localhost:11434` directly) is invisible
> to it — never claim "all local inference" coverage; that's a watcher-ladder item, not this one.
> **v1 scope, per the doc 28 §F1 build spec:** observe + gate on model IDENTITY only. Deterministic
> re-execution / strong-mode determinism is doc 28 phase 3 — not built, not claimed here. OMS/
> Sigstore manifest verification is a clearly-marked seam (phase 2), also not built.

| # | Feature | Status | Proof |
|---|---|---|---|
| F1.1 | `kriya-llm-proxy` — a governed HTTP reverse proxy (OpenAI-compatible `/v1/chat/completions`, `/v1/completions`, `/v1/embeddings` + Ollama-native `/api/chat`, `/api/generate`) in front of a local inference server; genuinely streams SSE/chunked responses (hashes bytes as they pass, never buffers a full body); every other path/method relays transparently | ✅ *(open runtime, `../experiment1` `feat/f1-local-inference`)* | `open:` `crates/kriya/src/llm/proxy.rs` — 20 unit/integration tests (request parsing, chunked bodies, streaming relay+hash, end-to-end governed completion, deny-tier refusal, ungoverned passthrough) |
| F1.2 | Model-identity resolution — reads a served model's digest from where it already lives: an Ollama registry manifest (`~/.ollama/models/manifests/...`, the layer Ollama itself computed), or an operator-precomputed file-hash cache keyed by `(path, size, mtime)`; **never hashes a multi-GB model file on the request path** — an unresolvable model is honestly reported `unresolved`, never guessed | ✅ | `open:` `crates/kriya/src/llm/manifest.rs` — 5 tests incl. a real Ollama-manifest fixture + file-cache round-trip |
| F1.3 | `kriya.model.identity` / `kriya.model.gate` / `kriya.model.serve` receipt vocabulary — first-sight identity, the policy verdict (every completion), usage/perf (`gen_ai.request.model`, `gen_ai.usage.input_tokens`/`output_tokens` per OT-1); prompt/output carried ONLY as sha256 hashes, never text | ✅ | `open:` `crates/kriya/src/llm/receipts.rs` (3 tests, incl. a no-content-field assertion) · `test:` console `test/otel-semconv.test.ts` no-content-leak coverage via the shared vocabulary-registry test |
| F1.4 | Model-identity policy (`ModelPolicy`) — an approved-digest allowlist per tier (allow/approval/deny, reusing `EgressTier`) + an `unknown_model` default action (`warn` \| `require-approval` \| `deny`); **fail-OPEN default** (`warn` — an unrecognized model is disclosed via an explicit signed receipt, never silently blocked); `require-approval`/`deny` are real enforcement once the operator opts in (no synchronous approval channel in v1 — an approval-tier model is refused, same honest limitation as `mcp::contain`'s CONNECT tunnel) | ✅ | `open:` `crates/kriya/src/permissions.rs` `ModelPolicy`/`ModelGateDecision` — 4 dedicated tests + YAML parity tests; lives in the SAME `agent-policy.yaml` every governed binary loads (optional `model:` key, BC-3 round-trip discipline) |
| F1.5 | OT-1 semconv mapping — `kriya.model.serve` is the first `kriya.*` vocabulary in the **`mapped`** bucket (real `gen_ai.*` attributes, not just `kriya.*`); `kriya.model.identity`/`kriya.model.gate` are `no_equivalent` with `kriya.model.*` extraction | ✅ | `test:test/otel-semconv.test.ts`, `src/lib/otel/{registry,semconv}.ts` · `cargo test -p kriya-verify otel::model_` (Rust twin, `src-tauri/crates/kriya-verify/src/otel.rs`) |
| F1.6 | Console "Local Models" view — every observed model (name, digest, digest source, approval state, serve count), built entirely from verified `kriya.model.*` receipts already in the audit trail (no separate backend call); one-click "Approve this digest" writes an `allow`-tier rule into `agent-policy.yaml` via the existing policy-save path | ✅ | `code:src/views/LocalModelsView.tsx`, `src/lib/policy.ts` (`ModelPolicy`, `approveModelDigest`) · `test:test/policy.test.ts` (model-policy YAML round-trip + `approveModelDigest` unit tests) · `shot:` manual proof below |
| F1.7 | End-to-end manual proof on this machine (no real local model available — an honest stub OpenAI/Ollama-compatible server stood in; see PR) | ✅ | two governed completions through `kriya-llm-proxy` (one unresolved digest, one Ollama-manifest-resolved digest) → all 6 receipts verify OK via `tools/verify-receipts` (0 chain breaks) → imported into the Console and re-verified independently by the TS verifier → "Approve this digest" exercised live in the UI |

### A9. Attested pipeline — Tier 1 pipeline + sandbox MEASUREMENT (F6-T1, doc 28 §F6, free)

> **This is measurement, not attestation — read `docs/TRUST.md`'s "Attested pipeline (F6-T1)"
> section before citing any row below.** Nothing here is rooted in hardware; a compromised host can
> lie about every field. Tier 2 (Linux TPM2/Keylime) and Tier 3 (`kriyad` SEV-SNP) are the gated
> upgrade path that would make a host-integrity CLAIM (not just an observation) honest — neither is
> scaffolded in this build.

| # | Feature | Status | Proof |
|---|---|---|---|
| F6.1 | `kriya.attest.pipeline` — sha256 self-hash of the Console binary, hashes of the connector-wired hook/gateway/hermes-hook binaries (`onboarding::resolve_*`), the active `agent-policy.yaml` hash, and a real macOS `codesign --verify` + parsed `Authority=` chain; every unresolved binary/file records the literal sentinel `"UNMEASURED"`, never a guessed value | ✅ | `test:` `measure_pipeline_writes_a_verifiable_chained_receipt`, `an_unresolved_binary_is_recorded_as_unmeasured_never_guessed`, `missing_policy_bundle_is_unmeasured_in_a_real_measurement`, `parse_authority_chain_extracts_authority_lines_only` (`src-tauri/src/attest/measure.rs`) · manual proof: `cargo run --example attest_demo` → real receipt incl. a real `codesign` invocation, re-verified via `cargo run -p kriya-audit-cli -- <path>` (exit 0) |
| F6.2 | `kriya.attest.sandbox` — mirrors the Seatbelt containment lane's (EG-C, doc 24 §11 B14) `kriya.io.run.start` bookend into the Console's own attest chain: profile hash + proxy port + the launched program's NAME/arg COUNT, **never its full argv**; idempotent per `scope_token`, including across app restarts | ✅ | `test:` `build_sandbox_params_extracts_config_knobs_never_full_argv`, `mirror_sandbox_receipts_is_idempotent_across_ticks_and_restarts`, `mirror_sandbox_receipts_matches_the_run_dash_prefix_naming_too`, `mirror_sandbox_receipts_ignores_non_run_files` (`src-tauri/src/attest/measure.rs`) · manual proof: same `attest_demo` run mirrors a synthetic `kriya.io.run.start` fixture |
| F6.3 | Both receipt families share ONE hash-chain (`~/.kriya/audit/attest.jsonl`, own key `~/.kriya/keys/attest.key`), excluded from Coverage Map lane classification (same convention as `coverage.jsonl`) so the Console's own self-chain never masquerades as a governed gateway lane | ✅ | `test:` `attest_jsonl_is_excluded_from_every_lane_not_just_local_stdio_mcp` (`src-tauri/src/coverage.rs`) |
| F6.4 | Drift surfacing — the Integrity card (Settings → Integrity) compares the two most recent pipeline measurements' binary hash and flags a change as an observation (never auto-blocked); fewer than two measurements is honestly "not enough history," never a fabricated "no drift" | ✅ | `test:` `compute_status_flags_drift_when_the_binary_hash_changes_between_measurements`, `compute_status_reports_no_drift_verdict_with_fewer_than_two_measurements`, `compute_status_flags_an_unchanged_binary_as_no_drift` (`src-tauri/src/attest/measure.rs`) · `code:src/views/SettingsView.tsx` (`IntegrityPane`) · manually verified rendering via `npx vite` in the PR session (no committed screenshot — no capture script targets Settings panes yet, see `docs/screenshots/CAPTURE.md`) |
| F6.5 | OT-1 vocabulary registration — `kriya.attest.pipeline`/`kriya.attest.sandbox` carry a decided `no_equivalent` OTel mapping in both languages (params surfaced as `kriya.attest.*` span attributes) | ✅ | `test:test/otel-semconv.test.ts` (registry + mapping) · `code:src/lib/otel/{registry,semconv}.ts`, `src-tauri/crates/kriya-verify/src/otel.rs` |
| F6.6 | One `kriya.attest.pipeline` measurement at app start + on demand ("Measure now" in Settings → Integrity); a background watcher mirrors sandbox sessions on a short poll | ✅ | `code:src-tauri/src/attest/measure.rs::spawn_watcher`, wired in `src-tauri/src/lib.rs` `.setup()` |
| F6.7 | Tier 2 (Linux TPM2/Keylime quote) + Tier 3 (`kriyad` SEV-SNP report) hardware-rooted attestation | 🧭 **gated, not built** | demand-gated per doc 28 §F6 — no code under `src-tauri/src/attest/` scaffolds either tier |

### A10. Signed artifacts — C2PA manifests bound to the receipt chain (F12a, doc 28, free)

> **Honesty note — read before claiming this row.** A public C2PA/CAI validator (or
> `kriya-audit --verify-artifact` run without this Console) correctly reports
> `signingCredential.untrusted` on every kriya-signed manifest — the artifact cert is a
> self-managed, self-signed anchor, never enrolled in any public trust list. This is provenance
> "for those who keep it" (a removable sidecar file), never a watermark, and never sold or
> documented as EU AI Act Art. 50 compliance (that duty falls on providers, not kriya's deployer
> customers). See [`TRUST.md`](TRUST.md)'s "Signed artifacts" section for the full honesty model.

| # | Feature | Status | Proof |
|---|---|---|---|
| F12a.1 | Sidecar manifest builder — a governed session's written file gets a `.c2pa` sidecar (never embedded) with producing session id, agent identity, policy-verdict COUNTS, and `kriya.chain.head` at emission time; content never in the manifest | ✅ | `test:` `kriya_assertion_json_is_deterministic_and_golden` (golden fixture), `cargo test -p kriya-verify --features artifact` · `code:src-tauri/crates/kriya-verify/src/artifact.rs`, `src-tauri/src/c2pa/mod.rs` |
| F12a.2 | Ed25519 signing via the pinned c2pa-rs 0.82.x, a dedicated artifact keypair generated once and persisted beside the Console's other on-device keys (`~/.kriya/keys/artifact.key`) + a self-signed cert (`artifact.cert.der`) built with the same rasn/rasn_pkix technique c2pa-rs's own `EphemeralSigner` uses — a generic-tool cert (rcgen, system `openssl`) was empirically found NOT to interoperate with c2pa-rs 0.82.1's own re-verification path | ✅ | `test:` `self_signed_cert_round_trips_pem_and_has_a_stable_fingerprint`, `identity_is_generated_once_and_stable_across_reloads` · `code:src-tauri/crates/kriya-verify/src/artifact.rs` (module doc comment has the full interop-failure writeup) |
| F12a.3 | The binding loop — manifest ⊃ `kriya.chain.head`; the emitted `kriya.artifact.provenance` receipt ⊃ manifest hash + target file hash + path, in its own free-tier self-attestation chain | ✅ | `test:` `export_then_verify_round_trips_for_a_governed_run` (Rust), manual proof below · `code:src-tauri/src/c2pa/mod.rs::emit_provenance_receipt` |
| F12a.4 | `verify_artifact` — c2pa validation (hard file-binding; an edited file fails) + honest trust reporting; `kriya-audit --verify-artifact <file> <sidecar>` — the offline stranger check, exit 0/1 | ✅ | `test:` `sign_then_verify_round_trips_the_manifest_and_assertion`, `edited_file_fails_verify_artifact_with_the_right_reason`, `corrupted_sidecar_fails_closed` (kriya-verify) + `verify_artifact_files_accepts_a_genuine_manifest`, `verify_artifact_files_rejects_an_edited_file`, `verify_artifact_files_fails_closed_on_missing_or_corrupt_files` (kriya-audit-cli) · manual proof: `cargo run --example f12a_manual_proof` then a real `kriya-audit --verify-artifact` run, exit 0 genuine / exit 1 tampered |
| F12a.5 | UI — SessionsView "Export provenance manifests" per session (discovers written files from verified receipts); Settings → Artifact signing (generate/show fingerprint, idempotent-but-honest) | ✅ | `test:` `filesWrittenByRun` suite (`test/sessionTree.test.ts`) · `code:src/views/SessionsView.tsx`, `src/views/SettingsView.tsx`, `src/lib/tauri.ts` |
| F12a.6 | OT-1 vocabulary registration — `kriya.artifact.provenance` mapped `no_equivalent` (a governance self-attestation, not a GenAI operation), both languages | ✅ | `test:test/otel-semconv.test.ts` (OT-1 registry test) · `code:src/lib/otel/{registry,semconv}.ts`, `src-tauri/crates/kriya-verify/src/otel.rs` |
| F12a.7 | Embedded manifests; F12b governed-commit attestation; public CAI trust-list enrollment; any watermarking claim | 🧭 **out of scope by design** | non-goals per doc 28 §F12 — see the module doc comment in `artifact.rs` |

### A11. Signed spend receipts — governed Claude Code session spend evidence (C1, doc 27, free)

> **Honesty note — read before claiming this row.** This reads local Claude Code transcript JSONL
> only: it does **not** see MCP broker-lane / tool-provider spend, direct API usage outside Claude
> Code, local-inference spend (the separate F1 lane), or any vendor-cloud/web Claude usage. The
> honest label is *"governed Claude Code session spend evidence,"* never *"complete"* or *"all"*
> spend. Costs are a **priced estimate** from a named, versioned, bundled sheet recorded in the
> receipt — never *"invoice"*/*"chargeback"*/*"the amount you were charged."* A model the sheet
> doesn't recognize is **unpriced** (tokens counted, cost withheld as `null`), never guessed. Deliberately
> a **different vocabulary and a different view** from the shipped byte/rate `budget` surface
> (`src/lib/budget.ts`/`BudgetView`) — "budget" stays bytes/rate-caps, "spend" is dollars; the two
> never share a noun. See [`TRUST.md`](TRUST.md)'s "Spend evidence (C1)" section for the full
> honesty model.

| # | Feature | Status | Proof |
|---|---|---|---|
| C1.1 | Two additive receipts — `kriya.spend.session` (one per transcript `sessionId`, once sealed; idempotent by `settle_fingerprint`, superseding a session that grew after a premature seal) and `kriya.spend.rollup` (a non-additive, referenced daily checkpoint — the panel's authoritative total is always the latest-per-session `session` receipts, the rollup is only cross-checked against it, never added) | ✅ | `test:` `settling_twice_at_the_same_fingerprint_emits_exactly_one_receipt`, `a_session_that_grows_after_settling_emits_a_superseding_receipt`, `rollup_sums_the_referenced_sessions_and_stays_non_additive` (`src-tauri/src/spend/mod.rs`) · `test:` `spend.test.ts` (`crossCheckRollup`, `latestSessions`) |
| C1.2 | Bundled, versioned, offline pricing sheet (`src-tauri/resources/pricing/claude-2026-07-01.json`, `include_str!`'d — fetches nothing) — five absolute per-token prices per model (input/output/cache-write-5m/cache-write-1h/cache-read), the sheet's own id+sha256 recorded in every receipt; an unrecognized model is `priced: false, cost_usd: null` (tokens still counted), never guessed or zeroed | ✅ | `test:` `pricing::tests` (unknown-model-None, cost-formula, sheet-version-bump ×5) + `an_unpriced_model_counts_tokens_but_withholds_cost_never_zero`, `a_pricing_sheet_version_bump_prices_the_same_usage_differently_each_recorded` (`src-tauri/src/spend/{pricing,mod}.rs`) |
| C1.3 | Rust-only extraction — reads ONLY `message.usage`/`message.model`/`message.id`/`requestId`/`sessionId`/`timestamp`/`isSidechain` from `~/.claude/projects/*/*.jsonl`; transcript content never crosses into the JS/TS layer; `project_hash` hashes the project directory's slugified NAME, never the raw cwd string | ✅ | `test:` `no_content_leak_transcript_text_never_enters_the_receipt` (sentinel secret + path fixture, asserted absent from the signed line) + `scan_and_aggregate_reads_project_dir_slash_session_dot_jsonl_and_hashes_the_project_slug` (`src-tauri/src/spend/mod.rs`) |
| C1.4 | Dedup by `(message.id, requestId)` (ccusage's exact discipline; `messages_deduped` recorded); subagent (`isSidechain`) turns rolled into the parent session's own totals, broken out under a `subagent` sub-object — never a separate per-subagent receipt (no double count); same `sessionId` surfaced under two project dirs keyed once, never by path | ✅ | `test:` `dedups_by_message_id_and_request_id_pair`, `subagent_turns_are_a_subset_breakout_never_a_double_count`, `same_session_id_across_two_project_dirs_is_keyed_once_never_double_counted` (`src-tauri/src/spend/mod.rs`) |
| C1.5 | Same signed-byte format as `coverage.rs`/`attest/measure.rs` (own chain `~/.kriya/audit/spend.jsonl`, own key `~/.kriya/keys/spend.key`) — re-proven by the SAME shared verifier, zero new trust machinery; excluded from Coverage Map lane classification (own chain, not a governed lane) | ✅ | `test:` `settled_receipts_verify_under_the_shared_trust_core_and_chain` (`kriya_verify::verify_value` round-trip + tamper→fail) · `test:` `spend_jsonl_is_excluded_from_every_lane_not_just_local_stdio_mcp` (`src-tauri/src/coverage.rs`) |
| C1.6 | `SpendView` — a NEW "Spend" nav entry (distinct from "Budgets & rate"), rendering ONLY from the verified `kriya.spend.*` rows the audit pipeline already loads: per-model + per-session cost tables, the active pricing-sheet id, unpriced-model callouts, and the rollup cross-check (flagged honestly on mismatch, never hidden) | ✅ | `test:test/spend.test.ts` (`summarizeSpend`, latest-per-session, unpriced surfacing, rollup cross-check ×13) · `code:src/lib/spend.ts`, `src/views/SpendView.tsx` |
| C1.7 | OT-1 vocabulary registration — `kriya.spend.session`/`kriya.spend.rollup` are the FIRST real `mapped` (non-`no_equivalent`) vocabulary decided up front in the registry: `gen_ai.usage.input_tokens`/`output_tokens` map verbatim; `gen_ai.request.model` ONLY when a session priced exactly one model (never for a multi-model session or a period-spanning rollup); cost/cache-tier/pricing-provenance stay `kriya.spend.*`-namespaced | ✅ | `test:test/otel-semconv.test.ts` ("kriya.spend.* (the mapped branch)" ×3) · `cargo test -p kriya-verify otel::spend_` (Rust twin, `src-tauri/crates/kriya-verify/src/otel.rs`, ×5) · `code:src/lib/otel/{registry,semconv}.ts` |
| C1.8 | Manual proof on this machine (real `~/.claude/projects` transcripts, ~65k usage-bearing messages across 1621 files) | ✅ | see the PR description for the exact totals, the offline re-verify (`kriya-audit-cli`) run, and the documented delta vs `npx ccusage@latest --json` |

### A12. ISO/IEC 42001 + CSA AICM evidence pack (D3, doc 27 §4, free)

> **A-number note:** C1 (signed spend receipts) merged first and claimed A11; per the shared-numbering
> plan this D3 pack is therefore **A12** (only the section number + row IDs changed on merge — no
> proof-cell content changed).
>
> **Honesty note — read before claiming this row.** kriya provides **evidence toward** ISO/IEC
> 42001:2023 Annex A operational sub-claims and CSA AICM v1.0 domains, **never** compliance of a
> control, **never** a certification, and **never** a third-party attestation — every Annex A row is
> capped at ◐ partial (never ✓ satisfied), and NOT-COVERED controls (risk/impact assessment,
> technical documentation, data quality/provenance, and more) are listed **explicitly**, because that
> honesty is the credibility feature (doc 27 D3, verbatim). See
> [`docs/design/d3-iso42001-pack.md`](design/d3-iso42001-pack.md) (the full mapping table + red-team
> pass) and [`TRUST.md`](TRUST.md)'s extended ISO 42001 row.

| # | Feature | Status | Proof |
|---|---|---|---|
| A12.1 | ISO/IEC 42001:2023 Annex A mapping — 13 rows (`A.6.2.8` event logs, `A.6.2.6` operation & monitoring, `A.9.4` intended use, `A.9.2` responsible-use process, `A.6.2.6+` crypto-module posture, all ◐; 8 explicit NOT-COVERED `✗` rows) + CSA AICM v1.0 domain mapping — 8 rows (LOG/A&A/GRC direct-or-supporting, IAM/DSP context-only, MDS/SEF + an 11-domain roll-up explicit NOT-COVERED), domain grain only (no fabricated per-objective IDs) | ✅ | `code:src/lib/compliance.ts::iso42001AicmControls` mirrored by `src-tauri/src/paid.rs::iso42001_aicm_controls` · `test:test/iso42001-sample.test.ts` (completeness + primary-rows-◐), `test/compliance.test.ts` ("ISO 42001 / CSA AICM evidence pack (D3)"), `src-tauri/src/paid.rs::framework_controls_tests` (21-row count, never-satisfied, samples-in-RESOLVABLE, empty-trail honesty) |
| A12.2 | New selectable export framework `ISO-42001-AICM` ("ISO 42001 / CSA AICM") — a SECOND, distinct `ReportsView` card alongside the existing narrower "ISO42001" (A.9) card; same signing/re-verification path as the AU pack (`evidence.md` + `evidence.json`, offline-re-verifiable via `kriya-audit`) | ✅ | `code:src/views/ReportsView.tsx` (`REPORTS` entry), `src-tauri/src/paid.rs::export_compliance` | 
| A12.3 | `ISO42001_SCOPE_BLOCK` (D-7) — the honesty ceiling embedded verbatim in every export, TS↔Rust byte-identical (parity test, same idiom as `EGRESS_SCOPE_BLOCK`); the explicit "NOT-COVERED (kriya emits no evidence for these)" section, test-asserted present | ✅ | `test:` `iso42001_scope_block_is_word_for_word_identical_to_the_ts_constant`, `iso42001_not_covered_section_present_in_markdown_only_for_the_aicm_framework` (`paid.rs`) · `test/iso42001-sample.test.ts` |
| A12.4 | AI-CAIQ v1.0.2 support sheet (D-5) — a third artifact (`ai-caiq-support.md`), self-assessment context only (fixed disclaimer, no answer/score column, org must fill from its own licensed copy); wired end-to-end in both `ReportsView.generate()` branches (Tauri `b.aiCaiq` + browser `renderAiCaiqSupport`) and the Rust `ComplianceBundle.ai_caiq` carrier, additive/omitted for every other framework | ✅ | `code:src/lib/compliance.ts::renderAiCaiqSupport`, `src-tauri/src/paid.rs::render_ai_caiq_support` · `test:` `ai_caiq_disclaimer_is_word_for_word_identical_to_the_ts_constant`, `ai_caiq_only_populated_on_the_iso_42001_aicm_arm` (`paid.rs`) · `test/iso42001-sample.test.ts` (disclaimer + UNVERIFIED markers) |
| A12.5 | Wording-lint (§4.3/§6.3) — all nine forbidden claim-inflation phrases (`compliant`, `certified`, `guarantees`/`guaranteed`, `STAR certified`, `third-party assess*`, `attestation of conformity`, `renders … MET`, `makes you compliant`), with the `certification`-noun carve-out; genuinely runs against the generated sample artifacts, not just source strings | ✅ | `test:test/iso42001-sample.test.ts` (`it.each` over evidence.md/evidence.json/ai-caiq-support.md) · `src-tauri/src/paid.rs::iso42001_aicm_evidence_never_contains_forbidden_claim_inflation_phrases` |
| A12.6 | Golden sample pack `docs/samples/iso42001-sample/` — real, generated (not hand-typed) from the same PII-free capture-audit fixture the AU pack uses, extended with a freshly-signed `kriya.crypto.module` attestation + one **deliberately tampered** receipt (proves the verify gate: `kriya-audit receipts.jsonl` genuinely reports 2 FAIL lines, exit 1 — the honest, expected verdict, not a bug) | ✅ | `script:scripts/gen-iso42001-sample.mts` (`npm run gen:iso42001-sample`) · manual proof: `kriya-audit docs/samples/iso42001-sample/receipts.jsonl` → `45/47 verified, 2 FAIL (lines 15 + 47), exit 1` |
| A12.7 | Fleet rollup (D-6, doc 22 boundary-scoped) — 3 ISO 42001 rows (`A.6.2.8`/`A.6.2.6`/`A.9.4`, fleet-wide) added to `fleet_evidence()`'s `OrgEvidence.controls`, computed from the SAME re-verified envelope rollups AU 3.3.1/3.3.4 already use (no new envelope field); "device-reported … fleet-wide … signed envelope rollups" wording, never "raw receipts"; the AI-CAIQ sheet is explicitly NOT rolled up (org-level, produced once) | ✅ | `test:` `d3_iso42001_fleet_rows_are_present_boundary_scoped_and_never_satisfied` (`fleet_evidence.rs`) · `test/orgEvidence.test.ts` (regenerated `sample-org-evidence.json` fixture, 12→15 controls) |
| A12.8 | Exact AICM objective-IDs, exact AI-CAIQ question text, third-party attestation of any kind, ✓-satisfied on any ISO 42001 Annex A control | 🧭 **out of scope by design** | copyright + UNVERIFIED (§1 of the design) / D-2 / D-5 — see `docs/design/d3-iso42001-pack.md` §10 |

### A13. Diode pack — the one-way evidence transport format (F10, doc 28 §F10, free)

> **Honesty note — read before claiming this row.** This is a TRANSPORT format, not a new
> cryptographic guarantee: every receipt inside a bundle still carries its own Ed25519 signature and
> hash-chain `prev_hash`, verified by the SAME `kriya_verify::verify_value` every other surface in
> this ledger uses. What's new here is the CONTAINER — a fixed-schema, signed, chunked directory with
> a whole-bundle Merkle root and an explicit continuity link to the previous export, built so a
> receiver behind a real data diode (zero backchannel, no ACK, no retry) can still detect a gap, a
> fork, or a missing frame. **FEC/Reed-Solomon reconstruction is NOT built (v2)** — a missing chunk is
> reported by index, never silently reconstructed or dropped. See
> [`docs/DIODE-BUNDLE.md`](DIODE-BUNDLE.md) for the wire format.

| # | Feature | Status | Proof |
|---|---|---|---|
| A13.1 | `kriya-diode-bundle/1` format — a directory bundle: signed `manifest.json` (fixed schema, versioned, no active content — statically inspectable) + `chunks/NNNNN.bin` (raw JSONL receipt batches, default 8 MiB/chunk, configurable), each chunk's sha256 declared in the manifest, plus a whole-bundle RFC-6962-style Merkle root over every included receipt line | ✅ | `code:src-tauri/crates/kriya-verify/src/diode.rs` (`DiodeManifest`/`ChunkEntry`/`build_chunks`) · `test:` `golden_two_receipt_bundle_hand_checkable_hashes` (chunk sha256 + Merkle root independently recomputed by hand in the test, not by calling the crate's own function again) |
| A13.2 | Continuity link — every manifest carries `chain_head` (this export's tail) AND `previous_chain_head` (the prior export's tail), so a zero-backchannel receiver holding just two manifests can detect a gap or fork between successive bundles without any query channel back to the sender | ✅ | `code:src-tauri/crates/kriya-verify/src/diode.rs::verify_bundle_dir` (continuity check), `src-tauri/src/diode/mod.rs` (state-file-tracked continuity across exports) · `test:` `gap_between_two_exports_is_detected_as_a_continuity_break`, `since_last_export_only_includes_new_receipts_and_chains_continuity` |
| A13.3 | Console action "Export diode bundle" — range `since-last-export` \| `date-range` \| `full`; `since-last-export` tracks a per-source-file cursor in `~/.kriya/console/diode-state.json` so repeated exports never re-send already-sent receipts; `full`/`date-range` are idempotent (the SAME range re-run over an unchanged audit dir is byte-identical in every chunk, hash, and the Merkle root — only `created_ms`, and therefore the signature, varies, documented exactly in `docs/DIODE-BUNDLE.md`) | ✅ | `code:src-tauri/src/diode/mod.rs::diode_export_bundle`, `src/views/SettingsView.tsx` (`DiodePane`) · `test:` `idempotent_rebuild_from_the_same_lines_is_byte_identical_except_created_ms`, `export_then_import_round_trips_for_a_governed_source` |
| A13.4 | Import + verify — `kriya-audit --verify-bundle <dir> [<prior-manifest.json>]` (the offline "stranger" check: manifest signature, chunk hashes, receipt verification, Merkle root, continuity) + the Console's own `diode_verify_bundle`/`diode_import_bundle` commands, both delegating to the SAME `kriya_verify::diode::verify_bundle_dir` so the CLI and the Console agree byte-for-byte; import is idempotent (a re-imported bundle is a detected no-op, never a duplicate) and partial-bundle tolerant (a missing chunk is reported by index; everything that DID arrive still verifies and imports) | ✅ | `code:src-tauri/crates/kriya-audit-cli/src/main.rs::verify_bundle_dir_cli`, `src-tauri/src/diode/mod.rs::diode_import_bundle` · `test:` `tamper_flipped_chunk_byte_fails_with_hash_mismatch_reason`, `tamper_manifest_signature_fails_with_signature_reason`, `tamper_forged_receipt_inside_a_correctly_hashed_chunk_fails_signature_verify`, `partial_bundle_reports_exactly_the_missing_chunk_indices`, `a_missing_chunk_is_partial_but_still_imports_what_arrived` |
| A13.5 | OT-1 vocabulary registry entries — `kriya.diode.export`/`kriya.diode.import` self-attestations (own dedicated on-device key, own dedicated chain file `kriya-diode-events.jsonl`, the same "the app signs its own governance-internal events" idiom coverage/otel/c2pa already use), both decided `no_equivalent` (no GenAI-semconv concept for a one-way transport bundle) with `kriya.diode.*` param extraction on both the TS and Rust sides | ✅ | `code:src/lib/otel/registry.ts`, `src/lib/otel/semconv.ts` (`diodeMapping`), `src-tauri/crates/kriya-verify/src/otel.rs` (Rust twin) · `test:test/otel-semconv.test.ts` (OT-1 parity, passes with the new entries present) |
| A13.6 | Spec doc for CDS/diode-filter vendors (Owl/Fend/Waterfall world) — structured, no active content, resumable, ACK-less — + a worked golden sample | ✅ | `docs/DIODE-BUNDLE.md` + `docs/samples/diode-bundle-sample/` |
| A13.7 | Real FEC/Reed-Solomon reconstruction of a lost chunk; QR-code transport; inbound (kriyad→device) policy-bundle direction over this same format; any network transport | 🧭 **out of scope by design (v1)** | FEC is v2 (doc 28 §F10); QR transport is A2's air-gap ceremony; inbound direction is a noted, unbuilt seam; this format is files-on-media only — no socket anywhere in `src-tauri/src/diode/` or `kriya_verify::diode` |
### A14. Endpoint budget gate — pre-execution USD spend enforcement (C2, doc 27 §4, free)

> **A-number note:** F10 (the diode pack, A13 above) merged first, so C2 takes the next free number,
> A14. Only the section number + row IDs changed on merge — no proof-cell content was altered.
>
> **Honesty note — read before claiming this row.** This is a **trailing-state** budget gate, **never**
> a hard cap: it blocks or routes-to-approval the *next* gated action after C1's Console-written
> spend state crosses an operator-authored USD threshold — the in-flight turn is never pre-counted,
> and a single expensive turn between extractor passes lands *after* the check. Every enforcement
> decision is a signed receipt recording the exact state timestamp it acted on. See
> [`docs/design/c2-budget-gate.md`](design/c2-budget-gate.md) (the full design + red-team pass) and
> [`TRUST.md`](TRUST.md)'s C2 section for the quantified staleness window.

| # | Feature | Status | Proof |
|---|---|---|---|
| A14.1 | Additive `budgets:` policy dimension (session / rolling-day / user scope, USD threshold, `deny`/`require-approval`/`warn` breach action, optional `on_missing_state` override) — BC-3 round-trip, mirrors `egress`/`detection`/`secrets`/`model`'s `Option<...>` discipline exactly | ✅ | `code:../experiment1 crates/kriya/src/permissions.rs` (`BudgetPolicy`/`BudgetRule`/`BudgetScope`/`BudgetBreachAction`/`OnMissingState`), `src/lib/policy.ts` (`parseBudgets`/`policyToYaml`) · `test:` `crates/kriya/src/permissions.rs` budget unit tests, `test/policy.test.ts` ("budgets — YAML round-trip") |
| A14.2 | Pre-execution gate consult placed in the hook `pre` lane immediately after the action-tier decision and BEFORE the credential-brokering block (F-B1, the B0 fix) — a budget can only ever ESCALATE an Allow/granted-approval, never loosen a prior Deny | ✅ | `code:../experiment1 crates/kriya/src/bin/kriya-hook.rs` (the C2 budget-gate consult, `kriya-hook.rs:711`-adjacent gap) · `test:crates/kriya/tests/kriya_hook_smoke.rs` regression matrix incl. `f_b1_budget_deny_short_circuits_before_credential_brokering_no_secret_injected` |
| A14.3 | Layered trailing-state freshness (D1): the always-on primary live-state file (`<state-dir>/spend-live.json`, C1's heartbeat, ≤1 tick ≈5 min) + the optional statusline accelerator (`kriya-spend-statusline`, ≈1 assistant turn), read-only, no-pricing-dependency runtime loader | ✅ | `code:src-tauri/src/spend/mod.rs::build_live_snapshot`/`spend_live_path`, `src-tauri/src/bin/kriya-spend-statusline.rs`, `../experiment1 crates/kriya/src/spend_state.rs` · `test:` `spend::tests::build_live_snapshot_*`, `spend_state::tests::*` |
| A14.4 | Fail-closed-by-default missing/stale-state matrix (D2) — `deny`→fail-closed, `require-approval`→route-to-approval, `warn`→fail-open, each overridable only by an explicit, receipted `on_missing_state` — extends B0, never weakens it | ✅ | `test:` `crates/kriya/src/permissions.rs` (`missing_state_on_a_*_rule_*`, `stale_state_is_treated_exactly_like_missing_state`, `an_explicit_fail_open_override_*`), `test/policy.test.ts` (`evaluateBudgets()` parity block) |
| A14.5 | Additive `kriya.spend.gate.{deny,approval,warn}` receipt vocabulary — signed on the existing hook `Signer`'s chain, carrying `state_source`/`state_as_of_ms`/`state_stale`/`pricing_sheet`/`pricing_sheet_hash`; OT-1 `no_equivalent` disposition (a governance enforcement decision, not a usage measurement) registered up front, TS↔Rust twin | ✅ | `code:../experiment1 crates/kriya/src/bin/kriya-hook.rs::budget_gate_params`, `src/lib/otel/registry.ts`+`semconv.ts` (`spendGateMapping`), `src-tauri/crates/kriya-verify/src/otel.rs` · `test:test/otel-semconv.test.ts` (auto-covers via registry), `kriya-verify::otel::tests::spend_gate_*` |
| A14.6 | Rolling-day/user day-attribution (F-MB) — each session's whole priced total attributes to the UTC day of its OWN `window_end_ms`, never split across midnight; honest-but-coarse, documented in TRUST.md | ✅ | `code:src-tauri/src/spend/mod.rs::build_live_snapshot` · `test:spend::tests::build_live_snapshot_attributes_rolling_day_and_user_by_window_end_ms_utc_day_fmb` |
| A14.7 | Naming discipline (Red-team a) — no "hard cap"/"guarantee"/"guaranteed"/"prevent"/"cannot exceed" anywhere in the budget lane's user-facing copy (PolicyView, SpendView, TRUST.md); permitted verb: "trailing-state budget gate" | ✅ | `test:test/policy.test.ts` ("C2 naming-check" — scans `PolicyView.tsx`/`SpendView.tsx`/`ApprovalsView.tsx` + TRUST.md's C2 section) |
| A14.8 | UX: "Spend budgets" authoring section (`PolicyView`), "Budget status" panel (`SpendView`), budget badge on routed `kriya.spend.gate.approval` approvals (`ApprovalsView`), the optional statusline install control with the N3 single-slot replace-warning | ✅ | `code:src/views/PolicyView.tsx`, `src/views/SpendView.tsx`, `src/views/ApprovalsView.tsx`, `src-tauri/src/govern.rs` (`install_spend_statusline`/`spend_statusline_status`) · `test:src-tauri/src/govern.rs` statusline unit tests |
| A14.9 | A live `$0.01`-budget deny demo, re-verified via selfverify, with the redacted receipt showing `threshold_usd`/`observed_usd`/`state_source`/`state_as_of_ms` | ✅ | manual proof: see the C2 build report's redacted `kriya.spend.gate.deny` receipt |
| A14.10 | Cross-machine fleet budget rollups, a synchronous org-wide spend cap, anything requiring a live network call to enforce | 🧭 **out of scope by design** | on-device, non-egress discipline (CLAUDE.md) — a future control-plane-tier item, not gated in this build |

### A15. Verified Replay — deterministic re-derivation of a session's timeline (B1, doc 27 §B1, free)

> **Honesty note — read before claiming this row.** "Verified Replay" is re-**DERIVATION** of already
> signed evidence, never re-**execution**: no tool is re-run, no LLM call is re-issued, no network
> request is re-sent, no counterfactual is simulated. The sibling item F4-wasm (deterministic
> re-execution) is a different claim and must never share wording with this one — see
> [`docs/design/b1-verified-replay.md`](design/b1-verified-replay.md) §4 and
> [`TRUST.md`](TRUST.md)'s "Verified Replay (B1)" section (copied verbatim from the design).

| # | Feature | Status | Proof |
|---|---|---|---|
| B1.1 | `derive_replay(sources, run_id)` — a pure function over already-non-filtering `load_rows` rows: sorts by `(ts_ms, source BYTEWISE, line_no)`, refuses (never skips) the WHOLE derivation on any in-scope signature/chain failure, derives `category`/`decision` by facet-parsing only (never from `success`), and renders FULL/ABSENT fidelity per field | ✅ | `code:src-tauri/crates/kriya-verify/src/replay.rs` (canonical) · `test:` `kriya-verify` `replay::tests::*` (12 tests: ordering/fidelity, tamper→signature refusal, tamper→chain refusal, not-found-vs-refused, decision-never-from-success, integer guard, multi-byte source ordering) |
| B1.2 | TS byte-parity mirror — `deriveReplay`/`canonicalReplayString`, reusing `loadAuditLog`/`readCorr`/`chainBreak`/`canonicalJsonString` (never a second verify/chain implementation); an explicit UTF-8-BYTE source comparator (`TextEncoder`), never `Array.sort()`/`localeCompare` | ✅ | `code:src/lib/replay.ts` · `test:test/replay.test.ts` (14 tests) |
| B1.3 | **Cross-language parity, hardcoded and CI-enforced**: the SAME checked-in golden fixture (`src-tauri/crates/kriya-verify/fixtures/replay-golden.jsonl` = byte-identical `test/fixtures/replay-golden.jsonl`) derives the SAME `canonical_replay_sha256` in both independently-written implementations — the equality is asserted as a literal hardcoded hash in both test suites, not just "both pass their own tests" | ✅ | `test:` `kriya-verify::replay::tests::golden_fixture_derives_the_hardcoded_cross_language_canonical_hash` ↔ `test/replay.test.ts` "the canonical_replay_sha256 matches the hardcoded value the Rust test also asserts" |
| B1.4 | The integer guard (F3 red-team fix) — `Number.isInteger`(TS)/integer-`Number`(Rust) THROWS on any non-integer number in the replay object, on top of the existing (insufficient) finite-only guard; negative fixture proves both languages throw identically | ✅ | `test:` `replay::tests::integer_guard_rejects_a_stray_float_and_accepts_integers` (Rust) · "1a: the integer-guard negative fixture" (TS) |
| B1.5 | The bundle (`<run>.kriya-replay.json`) — manifest + verbatim `logs` for every CONTRIBUTING source + the derived `replay` + the origin's own re-computable `verifier_verdict`; `build_bundle`/`buildReplayBundle` | ✅ | `code:` `kriya_verify::replay::build_bundle`, `src/lib/replay.ts::buildReplayBundle` · `test:` bundle round-trip tests, both languages |
| B1.6 | Two independent offline "stranger" verifiers, both re-deriving from the bundle's OWN `logs` (never trusting its `replay`/`verifier_verdict` claims): `kriya-audit --verify-replay <bundle>` (Rust CLI) and `verifyReplayBundle` (TS, the browser-side mirror) — both catch a hand-edited `replay` section even when the manifest's hash field was left correct, and both catch a tampered embedded log | ✅ | `code:src-tauri/crates/kriya-audit-cli/src/main.rs::verify_replay_bundle_cli` · `test:` `kriya-audit-cli` `verify_replay_bundle_cli_*` (4 tests) + `test/replay.test.ts` bundle-round-trip block |
| B1.7 | Console wiring — `derive_session_replay`/`export_replay_bundle` Tauri commands (free tier); the export defaults to `~/.kriya/exports/<run_id>.kriya-replay.json` when no path is given; emits the `kriya.replay.export` self-attestation (best-effort, chained, own key) | ✅ | `code:src-tauri/src/replay.rs` · `test:` `replay::tests::derive_session_replay_*`, `export_replay_bundle_*`, `export_refuses_on_a_tampered_source_*` |
| B1.8 | UI — `SessionsView`'s "Replay" stepper (prev/next/scrubber), the scope + non-claim banner fixed at the top, per-step fidelity chips ("— not recorded" for ABSENT, never fabricated), a "same ms" marker on tied timestamps, "Export replay bundle" button (hidden outside Tauri) | ✅ | `code:src/views/SessionsView.tsx` (`ReplaySection`/`ReplayStepper`/`FidelityRow`) |
| B1.9 | OT-1 vocabulary registration — `kriya.replay.export` registered `no_equivalent` in the REAL registries (TS `KRIYA_VOCABULARY`+`VOCAB_MAPPINGS`, Rust `EXACT_NO_EQUIVALENT`+`map_receipt`), params surfaced as `kriya.replay.{run_id,step_count,canonical_sha256}` | ✅ | `test:test/otel-semconv.test.ts` (OT-1 registry test, auto-covers) + dedicated `kriya.replay.export` mapping test, both languages |
| B1.10 | Naming-law enforcement — a dedicated wording-lint gate: forbids `re-run`/`re-execute`/`reproduce the result`/`simulate`/`replays the actions` across the shipped B1 surfaces (SessionsView copy, the `kriya-audit` CLI banner, the runtime code in `replay.ts`/`tauri.ts`'s B1 section) and requires "re-derivation, not re-execution" to be present, same "like C2's wording check" discipline the design specifies | ✅ | `test:test/b1-wording-lint.test.ts` (5 tests) |
| B1.11 | Cross-repo `kriya-audit` in the runtime repo (`../experiment1`), embedded-manifest replay, counterfactual "what would this look like under a different policy" (that's I3's job) | 🧭 **out of scope / resolved differently** | this build's `--verify-replay` lands in the Console's OWN `kriya-audit-cli` crate (the CLI that already hosts `--verify-artifact`/`--verify-otel`/`--verify-bundle`), not the runtime repo the design's file-list section named — see the B1 build report for the reasoning |

### A16. Deterministic Execution — the WASM re-execution lane (F4-wasm, doc 28 §F4, free)

> **A-number note:** B1 (Verified Replay, §A15 above) merged first, so F4-wasm takes the next free
> number, A16. Only the section number + row IDs changed on merge — no proof-cell content was altered.
>
> **Naming discipline.** "Deterministic Execution" = re-**EXECUTION**; kriya-console's own "Verified
> Replay" (B1) = re-**DERIVATION** from receipts. Never conflated — see TRUST.md's F4-wasm section.

| # | Feature | Status | Proof |
|---|---|---|---|
| A16.1 | Runtime `kriya-run-wasm` — executes a WASI-p2 component under pinned Wasmtime (`=41.0.4`, the newest line matching this repo's rustc-1.90 toolchain pin) with fuel metering, NaN canonicalization, threads/relaxed-SIMD off, and virtualized wall/monotonic clocks + both WASI random interfaces (seed + epoch as recorded inputs); `kriya-exec-bundle/1` record: module sha256, wasmtime version + config digest, args/env/stdin hashes AND bytes (size-capped at 4 MiB with an honest refusal, never truncation), fuel consumed, stdout/stderr hashes | ✅ | `code:../experiment1 crates/kriya/src/wasmexec/{mod,bundle,determinism,engine}.rs`, `crates/kriya/src/bin/kriya-run-wasm.rs` · `test:crates/kriya/tests/wasmexec_determinism.rs` (determinism/round-trip/tamper fixtures), `crates/kriya/src/wasmexec/*.rs` unit tests |
| A16.2 | `kriya-run-wasm --verify <bundle>` re-executes + hash-compares; console `kriya-verify::exec` is an independent Rust re-implementation of the identical check (one-way dependency law forbids a path dep between the repos — the two-copies-until-shared-crate convention `crypto.rs` already established for A4/A5), exposed as `kriya-audit --verify-exec <bundle> [--module <path>]` and a Tauri bridge command (`verify_exec_bundle`) — TS never re-derives a verdict | ✅ | `code:src-tauri/crates/kriya-verify/src/exec.rs`, `src-tauri/crates/kriya-audit-cli/src/main.rs` (`--verify-exec`), `src-tauri/src/wasmexec.rs`, `src/lib/tauri.ts` (`verifyExecBundle`) · `test:` `kriya_verify::exec::tests::*` (determinism/round-trip/tamper), `kriya-audit-cli` `verify_exec_bundle_cli_*` |
| A16.3 | Two example governed WASI-p2 tools (the rail is the product, not tool coverage): `text-transform` (upper/lower/reverse + `--show-clock`/`--show-hash` demonstrating the virtualized clock/RNG reach even non-obvious ambient calls) and `json-filter` (field/value array filter) | ✅ | `code:../experiment1 examples/f4-wasm-tools/{text-transform,json-filter}/` · `test:` exercised end-to-end in `wasmexec_determinism.rs`'s `json_filter_example_tool_filters_correctly_under_the_lane` |
| A16.4 | Policy tier `exec.prefer_deterministic_lane` + `exec.wasm_variants` (action_id → WASM module path) — an allowed action with a registered variant routes to the deterministic lane via a composing `WasmRoutingExecutor` (wraps the host's existing executor, zero changes to the governor's dispatch pipeline); either path signs a receipt (the normal action receipt, or that plus `kriya.exec.deterministic`) | ✅ | `code:../experiment1 crates/kriya/src/permissions.rs` (`ExecPolicy`, `Policy::resolve_wasm_variant`), `crates/kriya/src/wasmexec/routing.rs` · `test:` `permissions::tests::exec_*`, `wasmexec::routing::tests::*` |
| A16.5 | `kriya.exec.deterministic` receipt — binds only `bundle_hash` + summary fields (module hash, fuel, output hashes, success); never raw args/env/stdin/stdout content, C1 discipline | ✅ | `test:wasmexec::bundle::tests::receipt_params_never_carries_raw_input_or_output_bytes` |
| A16.6 | Cross-repo manual proof: a real `kriya-run-wasm run` on this machine, re-verified independently by BOTH `kriya-run-wasm --verify` and `kriya-audit --verify-exec` — same verdict, same fuel number, from two separately-implemented Rust cores; a one-byte-flipped bundle fails both with the matching hash-mismatch reason | ✅ | manual proof: see the F4-wasm PR description |
| A16.7 | rr/syscall recording (gated, Linux-later); general shell-command determinism claims; running third-party untrusted modules without the existing containment posture; native macOS syscall replay (rejected — no rr/PMU/SIP path; this WASM lane is the honest macOS answer) | 🧭 **out of scope by design** | non-goals per doc 28 §F4/§6 — see the module doc comment in `wasmexec/mod.rs` |

### A17. Fleet telemetry — kriyad speaks OTel (F3-fleet, doc 28 §F3-fleet, control-plane-licensed)

> **Envelope granularity, not per-receipt.** kriyad only ever holds signed envelope ROLLUPS (doc 22
> §2/§11-B1) — never individual receipts. So this item exports ONE SPAN PER ACCEPTED ENVELOPE, keyed
> on `kriya.envelope.device_pub` + `kriya.envelope.seq`, reusing F3's exact semconv-mapping primitives
> (`kriya-verify::otel`'s `attr_str`/`attr_int`/`base64_encode`/`sha256_hex`) — never a forked
> vocabulary. The originally-scoped per-receipt-signature design was rejected mid-flight by a
> STOP-on-contradiction (kriyad structurally cannot produce that data) — see the PR description for
> the full record.
>
> **Architecture decision:** the live OTLP push lives in the Console's `fleet-console` role
> (`src-tauri/src/control_plane/fleet_otel.rs`), not in `kriya-aggregator`/kriyad — kriyad's own crate
> doc states "no outbound calls" as an architectural invariant, and this module reuses the ALREADY
> `control-plane`-feature-gated `reqwest` dependency `fleet_client.rs`/`push.rs` link, rather than
> opening kriyad's first-ever outbound call. See the module's own doc comment for the full reasoning.

| # | Feature | Status | Proof |
|---|---|---|---|
| A17.1 | `kriya_verify::otel::map_envelope` — maps one accepted, already-verified `SignedEnvelope` to its OTel span attributes (`kriya.envelope.device_pub`/`seq`/`org_id`/`sig`/`raw`, `kriya.chain.head`/`prev_hash` via the SAME canonical-bytes convention `envelope_chain_break` uses, rollup counts/window/non_egress/policy_state as additional attributes) — the Rust twin, reusing (not forking) F3's `attr_str`/`attr_int`/`base64_encode`/`SCHEMA_VERSION` helpers | ✅ | `code:src-tauri/crates/kriya-verify/src/otel.rs` (the "F3-fleet" section) · `test:` `map_envelope_carries_device_seq_sig_and_rollup_counts`, `map_envelope_reuses_the_same_base64_and_schema_version_helpers_as_map_receipt` |
| A17.2 | `kriya_verify::otel::verify_envelope_span` — the envelope-mode sibling of `verify_span`: re-verifies via `kriya_verify::verify_envelope` (the SAME compiled verifier kriyad's ingest path uses), cross-checks chain-head/continuity using the identical convention `envelope_chain_break` expects | ✅ | `test:` `verify_envelope_span_accepts_a_genuine_chained_pair_and_checks_continuity`, `verify_envelope_span_chain_head_matches_envelope_chain_break_convention`, `verify_envelope_span_rejects_a_tampered_embedded_envelope`, `verify_envelope_span_rejects_a_forged_chain_head_even_with_a_valid_signature`, `verify_envelope_span_fails_closed_when_the_raw_attribute_is_missing` |
| A17.3 | `kriya-audit --verify-otel` envelope-mode branch — dispatches per-span on `kriya.envelope.raw` (F3-fleet) vs `kriya.receipt.raw` (F3), never both; a mixed export is supported | ✅ | `test:` `verify_otel_round_trips_a_genuine_accepted_envelope_sequence`, `verify_otel_fails_on_a_tampered_envelope_span_with_the_right_reason`, `verify_otel_fails_on_a_broken_envelope_chain_continuity` (`kriya-audit-cli`) · manual proof: real 2-envelope sequence ingested by a live `kriyad`, exported, `kriya-audit --verify-otel` exits 0 with "2 re-verified (2 OK, 0 FAIL)" — see the PR description |
| A17.4 | `build_fleet_export` — pure, network-free OTLP/HTTP JSON document builder: one span per accepted envelope, grouped one-trace-per-device, sorted `(device_pub, seq)` for deterministic/idempotent re-export (OT-5) | ✅ | `code:src-tauri/src/control_plane/fleet_otel.rs` · `test:` `build_fleet_export_emits_one_span_per_envelope_never_per_receipt`, `build_fleet_export_is_deterministic_and_idempotent`, `build_fleet_export_sorts_by_device_then_seq_regardless_of_input_order`, `build_fleet_export_skips_unparseable_or_malformed_rows_without_panicking` |
| A17.5 | `fleet_otel_export` Tauri command — pulls the fleet's envelope evidence via the existing P0/P5 windowed mTLS client (re-verified LOCALLY), then EITHER pushes live to the operator's OWN configured collector OR writes a file (network-free path) — OFF by default (`enabled: false`); requires `fleet-console` | ✅ | `code:src-tauri/src/control_plane/fleet_otel.rs::fleet_otel_export` · `test:fleet_otel_commands_dormant_without_a_fleet_console_license` (`src-tauri/tests/dormancy.rs`) |
| A17.6 | `kriya.otel.export.enabled` receipt, fleet-scoped (OT-3/OT-7) — a false→true transition on `fleet_otel_settings_set` signs one, idempotent on repeat enable, into its own chain (`kriya-fleet-otel-events.jsonl`) distinct from F3's per-device chain | ✅ | `code:fleet_otel.rs::emit_fleet_export_enabled_receipt`/`fleet_otel_settings_set` · covered by the dormancy test above (no signing before the license gate) |
| A17.7 | `GET /metrics` on kriyad — extended with AGGREGATE governance KPIs: `kriyad_receipts_total`/`kriyad_receipts_by_day` (per-UTC-day, capped 30 days), `kriyad_egress_allow/deny/approve_total`, `kriyad_device_coverage{device_pub,status}` (device-id only, never a hostname/username), `kriyad_policy_bundle_applied_devices{version}`/`kriyad_policy_bundle_latest_version` — every metric documented in its own `# HELP` line; route now requires an operator peer (a device cert is 403'd, matching `GET /v1/coverage`'s existing P6 access class) | ✅ | `code:src-tauri/crates/kriya-aggregator/src/store.rs::fleet_metrics`, `src/main.rs::metrics` · `test:` `fleet_metrics_aggregates_receipts_by_day_egress_and_coverage`, `fleet_metrics_reports_policy_bundle_applied_spread_and_latest_version`, `fleet_metrics_on_an_empty_store_returns_honest_zeros_never_panics`, `fleet_metrics_receipts_by_day_is_capped_at_max_metrics_days`, `day_key_matches_known_utc_calendar_days` (`store.rs`) · `metrics_forbidden_for_device_cert`, `metrics_includes_documented_fleet_kpis_for_operator` (`main.rs`) · manual proof: real `/metrics` scrape against a live `kriyad` with 2 genuinely ingested envelopes — see the PR description |
| A17.8 | `docs/samples/grafana-kriya-fleet.json` — an importable Grafana dashboard over the metrics above (fleet overview stats, receipts/day bar chart, governed-lane decisions, per-device coverage table, policy-bundle version spread), plus `docs/samples/grafana-kriya-fleet.md`'s README snippet | ✅ | `code:docs/samples/grafana-kriya-fleet.json` (validated well-formed JSON; Grafana-standard `__inputs`/`__requires`/`schemaVersion` shape) — a live Grafana import was NOT performed in this build's sandboxed environment (no Grafana instance available); see the honesty note in the PR description |
| A17.9 | Dormancy — every symbol in `fleet_otel.rs` compiles ONLY under `#[cfg(feature = "control-plane")]` (BC-1: reuses the SAME gate as every other `control_plane/` module, no new dependency to add to the dormancy build check); every command calls `require_fleet_console()` FIRST, before any disk/network/signing I/O | ✅ | `test:fleet_otel_commands_dormant_without_a_fleet_console_license` (`src-tauri/tests/dormancy.rs`) — proves no `fleet-otel-settings.json`, `kriya-fleet-otel-events.jsonl`, or network call happens without a license |
| A17.10 | Tempo/Jaeger trace-query APIs; storing spans in kriyad beyond existing evidence; any push to endpoints not operator-configured; per-user/per-hostname labels; free-tier surface changes of any kind | 🧭 **out of scope by design** | non-goals per doc 28 §F3-fleet — see `fleet_otel.rs`'s module doc |

### A18. Memory-write receipts — governed-lane persistent-memory evidence (D1, doc 27 §4, free)

> **Honesty note — read before claiming this row.** D1 is **observe + evidence, not blocking**; it
> covers **governed lanes only**, labels **only what the evidence supports as "memory,"** and
> commits **hashes, never content**. The provenance/trust-class badge is inert
> (`none-observed`) until the operator enables ingress recording — see
> [`TRUST.md`](TRUST.md)'s "Memory-write receipts (D1)" section (copied verbatim from the design).

| # | Feature | Status | Proof |
|---|---|---|---|
| A18.1 | New additive `action_id` family — `kriya.memory.write`/`.update`/`.delete` (`crates/kriya/src/audit.rs` consts) + the shared `crates/kriya/src/memwrite.rs` classifier/emitter: a closed four-class enum (`claude-md`/`claude-memory-dir`/`claude-settings`/`mcp-registered`), hash-only fields under a reserved `params["kriya.memory"]` sub-key (mirrors `kriya.corr`'s placement) | ✅ | `open:` kriya repo `crates/kriya/src/memwrite.rs` unit tests (classifier, hash-not-content sentinel test, prior-write-seen scan) |
| A18.2 | Hook-lane wiring — `kriya-hook.rs` (Claude Code) and `kriya-hermes-hook.rs` (Hermes) both classify + emit in `post`, unconditional (no policy opt-in, mirrors the base action receipt's always-on posture); `write`-vs-`update` from tool shape, upgraded to `update` only when a bounded (500-line) in-run tail scan finds an earlier write to the same path — `verb_basis` (`tool-edit`/`tool-write`/`prior-write-seen`) always discloses which | ✅ | `open:` kriya repo `tests/kriya_hook_smoke.rs` (6 D1 tests: per-class golden fixtures, prior-write-seen upgrade, hash-not-content, non-memory no-op) + `tests/kriya_hermes_hook_smoke.rs` (2 D1 tests) |
| A18.3 | Broker-lane `memory:` registry — `MemoryRegistryEntry {server, tool, op, content_field?}` in `crates/kriya/src/permissions.rs`, BC-3 unauthored round-trip (absent `memory:` ⇒ no broker call is ever treated as memory); `Governor::dispatch` emits for a REGISTERED tool only — an unregistered tool whose name looks like memory (`mcp__*memor*__*`, `*__save`, …) mints NOTHING (the honest default, never a name heuristic) | ✅ | `open:` kriya repo `crates/kriya/src/mcp/governor.rs` tests (`registered_memory_tool_call_emits_a_kriya_memory_write_receipt_hash_only`, `unregistered_memory_looking_tool_emits_no_kriya_memory_receipt_heuristic_honesty`) + `permissions.rs` registry parse tests |
| A18.4 | TS↔Rust parity — `d1-memory-receipts-ledger.jsonl`, a real Ed25519-signed fixture covering all four classes, committed byte-identical in both repos, verified by `src/lib/verify.ts` | ✅ | `open:` kriya repo `crates/kriya/tests/d1_memory_fixture.rs` · `test:test/memory.test.ts` ("D1 parity fixture" block, 5 tests) |
| A18.5 | Console view-model `src/lib/memory.ts` — VERIFIED-only per-target ledger (`path_hmac` for files, `server+tool` for MCP), the run-scoped provenance join over `kriya.io.ingress.*` (two-hop via the sibling action receipt's `kriya.corr.run_id`, since ingress receipts carry only a `corr` step-id pointer), derived reads, and advisory candidates (unregistered heuristic MCP tools + unconfirmable relative memory-dir paths) — never promoted to a signed receipt | ✅ | `test:test/memory.test.ts` (20 tests: provenance untrusted/trusted/none-observed + the F3 no-policy-configured default, heuristic-honesty, derived reads) |
| A18.6 | `MemoryView` — a new "Memory" nav entry (after "Spend"), per-target write timeline with verify ticks, the ⚠ untrusted-ingress banner naming the offending server + trust class, an advisory-candidates section, drill-in to Sessions | ✅ | `code:src/views/MemoryView.tsx` · manual proof: real `kriya-hook`-signed receipts (a genuine `CLAUDE.md` write + a memory-dir update, generated on this machine) re-verified through the production code path (`loadAuditLog` → `summarizeMemory`, the exact view-model this component renders) — both targets shown, 0 verification failures; a second sandboxed run with `egress.record_ingress: true` + a `scan`-classed server shows the badge escalate to "Untrusted ingress present," naming the server. **Honestly disclosed limitation:** no Tauri display was available in the build session, so this is the redacted receipt + ledger-state dump, not a `MemoryView` screenshot — see the D1 PR description |
| A18.7 | `.read` vocabulary (deliberately dropped — read volume + surveillance-shape, doc 27 §4 D-3); file-class delete detection (Claude Code Write/Edit cannot delete; Bash is opaque to the hook) | 🧭 **out of scope by design** | non-goals per `docs/design/d1-memory-receipts.md` §D-3 |

### A19. Temporal policy conditions — session-scoped preconditions on the governed-lane gate (B4, doc 27 §4, free)

> **Honesty note — read before claiming this row.** B4 is a **governed-lane, single-lane,
> workflow-ordering** gate: it reasons only over what the governed lanes actually recorded this
> session, on the lane the gated action itself ran on — never a proof of test coverage or code
> freshness, never a claim that spans lanes. Permitted claim: "blocks a governed action when no
> governed precondition receipt exists this session." Forbidden: "prevents untested pushes,"
> "guarantees tests ran," "cannot push without passing tests" — see
> [`TRUST.md`](TRUST.md)'s "Temporal policy conditions (B4)" section (copied verbatim from the
> design) and the D7 governed-corpus filter, which is a STRUCTURAL SUPERSET of the shipped,
> untouched `is_governance_internal` — zero change to any already-shipped behavior.

| # | Feature | Status | Proof |
|---|---|---|---|
| A19.1 | Additive `Policy.temporal: Option<TemporalPolicy>` (`crates/kriya/src/permissions.rs`) — four closed predicate forms (`happened`/`succeeded`/`count`/`since_minutes`) over a `Selector{action, command, outcome}`, `expect: satisfied\|unsatisfied` as the NOT operator, tighten-only (a matched `tier: allow` rule is always a no-op `Pass`); BC-3 (absent `temporal:` ⇒ byte-identical to pre-B4); the three hand-written `Policy` literals updated, compiler-enforced | ✅ | `open:` kriya repo `crates/kriya/src/permissions.rs` `temporal_tests` module (18 unit tests: all four predicates, tighten-only, first-match-wins, command substring matching, the receipt trace carrying no agent command content) |
| A19.2 | The B4 governed-corpus filter (D7) — `is_governance_internal_b4`, a STRUCTURAL SUPERSET of the shipped `is_governance_internal` (`is_governance_internal(id) \|\| (kriya.* && !is_governed_kriya_vocabulary(id))`), default-exclude so a future `kriya.*` family is excluded automatically; `is_governed_kriya_vocabulary` allowlists only `kriya.watch.{proc,file,net,dns}.*` (evidence) and `kriya.model.serve` — `kriya.watch.heartbeat`/`.run.start`/`.run.exit` correctly fall to governance-internal (watcher liveness, not agent activity) | ✅ | `open:` kriya repo `crates/kriya/src/session_cond.rs` unit tests (10 tests) + `crates/kriya/tests/temporal_fixture.rs` (F-C: this repo's own predicate matches every fixture-listed verdict + the superset property) · Console `src-tauri/src/policy_sim.rs` mirror (`is_governance_internal_b4`, `is_governed_kriya_vocabulary`) with its OWN F-C test + a regression pin proving the shipped `is_governance_internal` still returns exactly its four historical verdicts |
| A19.3 | The session-scoped fold + cache (`crates/kriya/src/session_cond.rs`, NEW) — `fold_receipts` verifies every folded receipt (a local `ReceiptForVerify` + `crypto::verify`, no `kriya-verify` dep) and drops anything not from this run or B4-governance-internal; `load_or_build` is a cursor-based cache under C2's own `state_dir_for_audit_log` seam (`<state-dir>/session-cond/<run_id>.json`), tail-appending on a cache hit, fully rebuilding on any cache/log mismatch, `Err(Unavailable)` only on a genuine I/O error (a missing/empty log is a valid empty session, not unavailable) | ✅ | `open:` kriya repo `crates/kriya/src/session_cond.rs` tests incl. a real chmod'd-unreadable-log scenario and a real log-truncation-forces-rebuild scenario |
| A19.4 | Hook-lane wiring — `kriya-hook.rs`'s `pre` gate, inserted between the action-tier `Decision` and the C2 budget gate; `on_unavailable` (`fail-closed`/`fail-open`/`require-approval`, per-tier default) handled when the fold itself can't be computed; signs `kriya.policy.cond.deny`/`.approval` (ids/patterns/counts/timestamps only — never the agent's command text) | ✅ | `open:` kriya repo `crates/kriya/tests/kriya_hook_smoke.rs` — 6 new real-process rows incl. **the acceptance-criterion-1 demo**: `git push` denied with no prior test, the SAME push allowed after a real signed `npm test` success this session, and fail-closed-on-an-unreadable-session-log |
| A19.5 | Three-twin parity — runtime `TemporalPolicy::evaluate` ↔ Console `kriya-verify::simulate_temporal` ↔ Console TS `evaluateTemporal`, over three MIRRORED, committed fixtures: **F-A** (the fold — two agent bash events survive a mix with `kriya.spend.gate.warn`/`kriya.memory.write`/`kriya.artifact.provenance` sharing the same run_id, all real Ed25519-signed receipts), **F-B** (the evaluators — a wildcard `count()` rule), **F-C** (the predicate + its superset property) | ✅ | `open:` kriya repo `crates/kriya/tests/temporal_fixture.rs` (5 tests) · Console `src-tauri/crates/kriya-verify/src/simulate.rs` (19 tests incl. the F-B loader) · Console `test/policy.test.ts` (evaluator parity + the F-B loader, 20 tests) |
| A19.6 | I3 Policy CI extension — `src-tauri/src/policy_sim.rs`'s temporal replay groups windowed verified receipts by `(source log, run_id)`, folds per lane (never cross-lane, D7's "second divergence"), reports `temporal_fired` + `temporal_by_rule` + a `temporal_corpus_note` when the two corpora deliberately diverge; additive `temporal_fired` on the signed `kriya.policy.sim.result` receipt | ✅ | `code:src-tauri/src/policy_sim.rs` `policy_sim::tests` (16 tests) incl. a live end-to-end replay over real signed receipts (acceptance criterion 2) and a cross-lane-scoping test proving a same-run_id success on a DIFFERENT lane does NOT clear the rule |
| A19.7 | `PolicyView` form-based authoring (never free-text — the closed predicate set stays closed by construction): per-rule subject selector, tier, `when:` condition builder, `on_unavailable`; lints for a `tier: allow` no-op and a command-less Bash `count`/`since_minutes` selector; the "Test before apply" simulator surfaces `temporal_fired` + the corpus-divergence note. `ApprovalsView` condition badge on a routed `kriya.policy.cond.approval` renders each condition as `predicate(selector) → observed` | ✅ | `code:src/views/PolicyView.tsx`, `src/views/ApprovalsView.tsx` · `test:test/policy.test.ts` (BC-3 round-trip, both lints) |
| A19.8 | Pre-existing OT-1 registry gap closed as its own commit (`kriya.exec.deterministic`, `kriya.memory.{write,update,delete}`, `kriya.retention.checkpoint` — three shipped vocabularies with no registry entry, found while auditing every `kriya.*` family for D7) + B4's own `kriya.policy.cond.*` registration | ✅ | `test:test/otel-semconv.test.ts` (registry-completeness) · `test:test/governance-filter.test.ts` (NEW — the F-C staleness check itself) |
| A19.9 | `.warn` reserved, not emitted (M-1) — `TemporalDecision` has no `Warn` variant in v1; observe-only is a documented v2 lever | ✅ | `open:` kriya repo `kriya_hook_smoke.rs::temporal_cond_warn_is_reserved_not_emitted_in_v1` + `permissions::temporal_tests::a_matched_allow_tier_rule_is_always_a_no_op_pass` |

### A20. Behavioural baselines — the local drift sentinel (B3, doc 27 §4, free)

> **Honesty note — read before claiming this row.** B3 is a **descriptive, on-device, non-egress**
> statistic: for four receipt-derived distributions (tool mix, unsuccessful outcomes, egress-class
> mix, spend shape), it says *"this stream's recorded mix has shifted versus its own earlier
> baseline, by this much, driven by this category"* — never an anomaly/security/quality verdict, and
> never enforced. The false-alarm level (α) is a **pre-registered threshold**, never quoted as a rate
> or percentage on the card (T11 enforces this mechanically). On a bursty real stream the realised
> false-alarm rate is measured at **~20–30× α** — see [`TRUST.md`](TRUST.md)'s "Behavioural baselines
> (B3)" section, which states this with the actual measured numbers, not a rounded-away qualifier.
> The receipt namespace is `kriya.drift.*` (OT-2); the UI never says "drift" — that word already
> means policy-**bundle** drift on the fleet screens (`policyDrift.ts`), a different object entirely.

| # | Feature | Status | Proof |
|---|---|---|---|
| A20.1 | The statistic (D6/D7) — a **pooled conditional predictive** sequential Bayes factor (Ville/e-value test martingale), **not** a plug-in estimate: `p_i(k) = (2b_k+2c_k+1)/(2B+2(i-1)+K)`, `E_n = KT(b)·KT(x)/KT(b⊕x)`, `E[E_n\|b] = 1` **exactly** (proved by exhaustive rational-arithmetic enumeration, zero floating point, at 6 parameter cells — 4 named by the design + 2 chosen independently). Parity-safe arithmetic: integer operands, exactly two IEEE double ops per step in a fixed order, no `ln`/`exp`/`pow`; every receipted number (`e_milli`/`tv_milli`/`threshold_milli`/`alpha_milli`) is an integer, `e_milli` capped at `2^31-1` — confirmed **load-bearing, not dead code**, in a real run on this machine (see A20.5) | ✅ | `code:src-tauri/crates/kriya-verify/src/drift.rs` (canonical, 11 tests incl. T1/T2 fixture goldens) · `code:src/lib/drift.ts` (TS mirror) · `test:scripts/drift-sim.mts --exact` (T14(a), 6 cells, milliseconds) |
| A20.2 | D2 — the monitored corpus: `tool_mix`/`outcome_mix` fold verified rows not excluded by `is_behaviour_corpus_excluded` (console `policy_sim.rs`), a structural superset of B4's `is_governance_internal_b4` plus B3's own two narrowing arms (`kriya.watch.*`, `kriya.model.serve` — excluded here for a PROPORTION question even though B4 counts them for its PRESENCE question); the whole predicate collapses to a bare `id.starts_with("kriya.")` check for every id (proved, not asserted), which is why `kriya.drift.*` is excluded from its own corpus automatically. `egress_class_mix` folds the closed 24 `kriya.io.*` facets; `spend_shape` reads C1's own `latestSessions()` reducer, never a second parser | ✅ | `code:src-tauri/src/policy_sim.rs::is_behaviour_corpus_excluded` (3 new tests) · `test:test/drift.test.ts` T4 (enumerates `KRIYA_VOCABULARY` + a synthetic unregistered id, never a hand-copied list) |
| A20.3 | D5 — alphabets frozen at arming: `tool_mix`/`egress_class_mix` top-12 by descending baseline count (UTF-8 byte-order tie-break) + mandatory `__other__`; `outcome_mix` fixed `["success","failure"]`; `spend_shape` fixed cost bins `b0..b4` on exactly-representable binary edges. D8 — no unsigned state anywhere: every evaluation replays from the corpus + this stream's own prior `kriya.drift.baseline` receipts (the one externally-injected fact: WHERE an operator manually rebaselined) | ✅ | `test:test/drift.test.ts` (alphabet freeze, COLD honesty, spend unpriced 3-case discriminator incl. the partially-unpriced regression F5 caught) |
| A20.4 | The committed measurement harness (`scripts/drift-sim.mts`) — imports the REAL `src/lib/drift.ts` detector (never a reimplementation): SplitMix64 (checked-in, deterministic, never `Math.random`), the ONE burstiness generator (D8: one sticky chain, baseline = the stream's own first B observations — NOT an i.i.d. baseline spliced onto a bursty monitor, the defect a prior design revision had to fix), FA/lag sweeps, T1/T2 fixture generation. Re-measured independently on this machine: burstiness FA at ρ=0.3 = **20.3% (Zipf) / 30.0% (uniform)** at 3 000 streams/cell (design: 21%/29% at 40 000/cell) — within Monte-Carlo noise of both the design's and the independent re-review's numbers | ✅ | `test:test/drift-sim.test.ts` (T14(a)-(e): exactness, FA≤α, detection lag p90≤120/median≤60, m_min monotonicity, the burstiness FLOOR ≥10%×α) — 7 tests, ~8s · `open:npm run drift:sweep` (full-scale sweep) |
| A20.5 | The emitter (`src-tauri/src/drift/mod.rs`) — transitions-only signing (D9: `COLD→STABLE`/roll/manual-rebaseline `kriya.drift.baseline`, `STABLE→SHIFTED` `kriya.drift.observation` once, never per-tick), idempotent by `(epoch_id, state)` tail-scan, own chain + own key (`coverage::load_or_create_key`). **Acceptance run on this machine, real Ed25519-signed receipts, real `verify_value`/`chain_break`:** a COLD→STABLE→SHIFTED→idempotent-no-op→operator-rebaseline lifecycle measured **`e_milli = 2147483647` (exactly `2^31-1`) — the cap binding for real**, `tv_milli = 1000` ("mix moved 100%"), `monitor_n = 30` (= `m_min`, the fastest possible alarm on a totally novel category) | ✅ | `code:src-tauri/src/drift/mod.rs` (8 tests incl. `acceptance_real_signed_receipts_verify_end_to_end`, T5/T6/T7×2/T9) · `code:src-tauri/src/coverage.rs` (T8, drift.jsonl lane exclusion) |
| A20.6 | `MonitorView`'s "Behavior baselines" card — one row per armed stream×dimension, the armed-baseline count (multiplicity disclosure), receipt-backed status + live TS recomputation with a loud mismatch flag, a Rebaseline button on SHIFTED rows. **Verified against a real imported corpus in the running app** (267 real signed receipts, 2 streams): correctly showed `claude-code`/tool_mix as `SHIFTED (new category)`, `claude-code`/outcome_mix as `STABLE` (the SAME corpus, different projection — confirming dimension independence), and `hermes-hook`'s two dimensions as `BASELINE FORMING — 37 of 150` | ✅ | `code:src/views/MonitorView.tsx` · `code:src/lib/tauri.ts` (`driftStatus`/`driftRebaseline` bindings) · view-model evidence: live DOM read of the running app (not a committed screenshot — see the PR description) |
| A20.7 | The naming law (D1) — receipt namespace stays `kriya.drift.*` (OT-2); the word "drift" is BANNED from every rendering surface (enforced, not just asked for). The card may NEVER quote α as a rate/percentage anywhere — T11 widened from a 2-string check to a general percent-literal regex scan (`/\d\s*%/`) + `0.01`/`1/100`/`α`/`alpha` literals + "false-alarm" adjacent to a digit. The ONE percentage the card may carry is the computed `tv_milli` effect size, labelled as what moved, never how often | ✅ | `test:test/b3-wording-lint.test.ts` (4 tests) · `doc:docs/TRUST.md` "Behavioural baselines (B3)" section |
| A20.8 | OT-1 registration — `kriya.drift.observation`/`.baseline`, both `no_equivalent`, in all three surfaces (`registry.ts`, `semconv.ts`, `kriya-verify/src/otel.rs`) | ✅ | `test:test/otel-semconv.test.ts` (registry-completeness) · `test:test/governance-filter.test.ts` (F-C staleness — B3's 2 ids added to the mirrored fixture in both repos) |
| A20.9 | Runtime touch — fixture-only, no new runtime source: `kriya.drift.observation`/`.baseline` added under `internal` to the mirrored `governance-filter-ids.json` in both repos (B4's own namespace default-exclude covers them with zero predicate change) | ✅ | `open:` kriya repo `crates/kriya/tests/temporal_fixture.rs` (regenerator list updated so its OWN test doesn't clobber the fixture on a future `cargo test`) |

### A21. Shift attestation — signed unattended-window reports + the armed fail-closed clamp (F-6, doc 31 / doc-25 I4, free)

> **What it proves, stated honestly.** The **report** is signed, offline-verifiable evidence of
> what a governed agent did across an unattended window, plus heartbeat **gaps** where the window
> went unwatched — measurement, not a promise nothing happened off-lane. The **clamp** is real
> enforcement, but only on the governed Claude Code hook lane and only while **armed**: state absent
> ⇒ no clamp; a state-write failure never blocks a gate (the "cache is never the source of truth"
> law). Tighten-only — it can drop an action to a stricter tier, never loosen a deny.

| # | Feature | Status | Proof |
|---|---|---|---|
| A21.1 | Signed shift reports — the Console folds verified rows into a `kriya.attest.shift.report` (its own key/chain, closed action-id) plus `kriya.attest.shift.gap` receipts for heartbeat gaps `>2×` the median cadence; `arm_shift`/`disarm_shift`/`shift_state` write `~/.kriya/state/shift.json` (armed · window · fail_tier) via the read-only-state pattern | ✅ **v0.4.0** | `test:shift.test.ts` ×10 (fold → report body, gap detection at `>2×` median) + `test:shift-receipts.test.ts` ×3 · `artifact:test/fixtures/shift-receipts.jsonl` (Rust-signed, TS-verified **byte-identical**) · `code:src/lib/shift.ts`, `src-tauri/src/attest/shift.rs` |
| A21.2 | The armed fail-closed clamp (runtime) — `shift_guard.rs` (pure) reads `shift.json` + a bounded heartbeat tail; **armed + a missed beat inside the window ⇒ the tier is clamped** (Approval → gate, Deny → exit 2), each decision signing a `kriya.shift.clamp` evidence receipt; wired into `kriya-hook`'s pre-decision | ✅ **v0.4.0** | `open:` `crates/kriya/src/shift_guard.rs` — 6 unit tests (armed missed-beat clamps · tighten-only · state-absent no-clamp · bounded tail) + `kriya_hook_smoke` shift e2e (real binary: a missed beat blocks an otherwise-allowed action w/ a signed clamp; a fresh beat proceeds) |
| A21.3 | Console surface — Compliance › **Shift reports** (free tier: arm / disarm, compose, list w/ hero numbers + an amber gap band + receipt drawer) + the **Today Overnight-shift card** live (the F-1 placeholder replaced; the Evidence view byte-untouched) | ✅ **v0.4.0** | `code:src/views/*` (Shift reports view, Sidebar/App/palette registration) · `shot:docs/screenshots/shift.png` (signed report + amber gap band) + `docs/screenshots/today.png` (the Overnight-shift card) |
| A21.4 | OT-1 registration — `kriya.attest.shift.{report,gap}` + `kriya.shift.clamp` carry decided mappings in both languages; all three dispositioned governance-internal in the F-C `governance-filter-ids.json`, **byte-identical** across repos | ✅ **v0.4.0** | `test:test/otel-semconv.test.ts` (registry + mapping) · `test:test/governance-filter.test.ts` (F-C staleness) · `open:` `crates/kriya/tests/temporal_fixture.rs` (F-C disposition regen) |

---

## PART B — EGRESS (outbound governance — shipped in v0.2.4)

> The egress set was built to full competitive parity **before** being sold — no row was claimed
> until its proof cell was real. As of **Console v0.2.4** the whole set below ships in the notarized
> DMG, in the free tier. B19 (deeper host rungs) is the one genuinely-later row and is marked so.
>
> Every capability is built on the signed `kriya.io.<direction>.<kind>.<decision>` ledger (a closed
> id set), the egress policy tier, allowlist-enforced redaction, and the Policy/Coverage UI.
> **The honest ceiling ships with the features:** the controls cover **governed lanes** (hook ·
> gateway · broker) plus anything launched under **containment** (B14) — a raw-socket bypass
> outside a contained session is stated first, unprompted, and the Coverage Map shows it. Compliance
> claims stay stricter than the marketing verbs: SC-7 / 3.13.6 appear in an export only after a
> per-assessor validation, even though the contained code enforces.

| # | Feature | Status | Proof |
|---|---|---|---|
| B0′ | Self-verifying egress-receipt demo — one HTML file, embedded verifier, tamper-a-byte over `kriya.io.*` receipts | ✅ **v0.2.4** | `artifact:docs/samples/egress-receipt-demo/` (self-verifying HTML + `receipts.jsonl`) · `test:selfverify.test.ts` (chain-check, trust-spine) |
| B1 | Egress allowlist / deny-by-default by destination host + kind | ✅ **v0.2.4** | runtime fixture `kriya-verify/fixtures/runtime-egress-ledger.jsonl` (verified by the kriya-verify suite) · `shot:docs/screenshots/policy-egress.png` (host tiers + deny-by-default round-tripping into enforced YAML) |
| B2 | Per-destination byte budgets + rate limits (anti slow-drip) | ✅ **v0.2.4** | budget-tier enforcement over observed payload bytes (runtime egress tests; L2 honesty label on observed-bytes) |
| B3 | **Fail-closed receipt-precondition — "no receipt, no egress"** ⭐ the kriya-native differentiator: the proof is the gate | ✅ **v0.2.4** | receipt write fails ⇒ egress denied (runtime governor tests) · demonstrated in the B0′ self-verifying artifact |
| B4 | Ask / defer approvals on egress (park unlisted destinations for a human) | ✅ **v0.2.4** | `require-approval` tier per destination + `kriya.io.*.approve` ids in the closed set (`code:src-tauri/src/control_plane/redact.rs`) · `shot:docs/screenshots/policy-egress.png` |
| B5 | DNS-exfil + anomalous-destination + subdomain-entropy detection | ✅ **v0.2.4** | detection pack in the runtime governor (alert-or-deny per policy); Policy UI rows |
| B6 | SSRF / private-IP / cloud-metadata / DNS-rebinding blocking (resolve-then-pin) | ✅ **v0.2.4** | guard on governed lanes (`code:permissions.rs`/`governor.rs`); adversarial network tests |
| B7 | Credential + secret + PII scanning & redaction on outbound bodies (hash + match-type stored, never the secret) | ✅ **v0.2.4** | redaction manifest (`test:redaction_manifest.rs`) + the customer privacy pack ([`privacy/`](privacy/)) |
| B8 | Operation rails — allow/deny specific API operations (HTTP verb/path, GraphQL mutations); parse-fail ⇒ deny | ✅ **v0.2.4** | rail enforcement incl. parse-failure fail-closed (runtime egress tests); Policy UI rows |
| B9 | Canary tokens — planted string ⇒ immediate deny + loud alert | ✅ **v0.2.4** | canary trip → signed alert receipt (runtime governor) |
| B10 | Connector registry — new MCP server/tool **disabled-until-approved**; tool-description drift/poisoning scan | ✅ **v0.2.4** | registry state + drift-scan (broker + Policy UI) |
| B11 | Per-connector / per-tool enable-disable + read-only rails | ✅ **v0.2.4** | per-action tier on the existing policy engine; Policy UI read-only presets |
| B12 | MCP response enforcement — block-by-default responses + per-server trust classes | ✅ **v0.2.4** | response-gate on governed MCP lanes; trust-class rows |
| B13 | Credential brokering — agent holds a placeholder; real secret injected at egress (keychain / Secure-Enclave custody) | ✅ **v0.2.4** | [`THREAT-MODEL-brokering.md`](THREAT-MODEL-brokering.md) (its own trust posture) + brokering path in the gateway |
| B14 | OS containment (macOS) — launch-under Seatbelt sandbox + recording CONNECT proxy forces agent traffic through the governed lane; turns *observe* into **enforce** for the governed subtree | ✅ **v0.2.4** | `kriya-gateway run -- <agent>`; contained sessions light the raw-egress Coverage lane; enforcement verbs earned for the subtree only |
| B15 | Spend / budget caps | ✅ shipped | see A1.7 |
| B16 | Fleet egress — policy distribution + stale-policy kill-switch + fleet receipt report | ✅ **v0.2.4** | egress policy/budgets/kill-switch in the signed PolicyBundle (rides A5.5); fleet egress-receipt report |
| B17 | Broad agent coverage | ✅ shipped | see A1.10 — the native hook seam (agent *decisions*) no proxy has |
| B18 | A2A (agent-to-agent) governance | ✅ **v0.2.4** (thin) | broker A2A-lane seam + PolicyBundle convergence proof |
| B19 | Deeper host rungs — Linux eBPF/Tetragon host observation · macOS host-wide enforcement (Apple ES entitlement) | 🧭 roadmap | after containment proves the model; not built, not claimed |

**Egress compliance truth (fixed, not negotiable):** the governed-lane ledger honestly supports
AC + CM + SI *slices* (3.1.3 ◐, 3.4.2 scoped, 3.14.6/7 ◐, AC-4 ◐, SI-4 feeds-never-is, SOC 2
CC6.1/CC6.7/CC7.2 ◐, Art. 12 readiness, DORA 28–30). **Not claimed at that layer:** 3.13.1, 3.13.6,
SC-7, SC-8, CC6.6. Containment (B14) lets the *code* earn SC-7-monitor→control and 3.13.6 for the
contained subtree; the *export claim* still waits for assessor validation. Never claim "DLP" or
"firewall" unqualified.

---

## Maintenance rules

1. A row flips ✅ only with a real proof cell (test merged + artifact/screenshot where applicable).
2. New feature ⇒ new row **here first**, then FEATURES.md, then the rest of the collateral.
3. Screenshots regenerate via `npm run capture` / `npm run capture:fleet`; keep `shot:` paths live.
4. If TRUST.md and this file ever disagree, TRUST.md wins and this file has a bug.
